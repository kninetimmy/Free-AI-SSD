# Screenshot drop folder

Save Mac app screenshots here with these **exact** filenames. The callouts in
`../RECONSTRUCTION.md` already reference them — match the names and the doc
renders complete, no rename step. Keep window title / step label in frame.

## Prep (mac-prep-app)
- `prep-01-welcome.png`
- `prep-02-drive-selection.png`  (grab the configured-drive banner too if possible)
- `prep-03-erase-confirmation.png`
- `prep-04-formatting.png`        ⚠ looks like 05 & 08 — keep the title visible
- `prep-05-staging.png`           (optional) ⚠ looks like 04 & 08 — keep the title visible
- `prep-06-encryption-setup.png`  (encrypt toggle OFF)
- `prep-06b-model-picker.png`     (expand a Hugging Face repo to show quants)
- `prep-07-model-pull.png`
- `prep-07a-pull-paused.png`      (optional — needs a cancel)
- `prep-07b-pull-failed.png`      (optional — needs a failure)
- `prep-08-readiness.png`         ⚠ looks like 04 & 05 — keep the title visible
- `prep-09-manage-models.png`     (locked + unlocked banner states if you can)
- `prep-10-unlock-sheet.png`      (optional)
- `prep-11-done.png`
- `prep-12-failed.png`            (optional — needs a failure)

## Runner (mac-runner)
- `runner-01-main.png`            (after unlock, model selected)
- `runner-02-documents.png`       (Network Mode ON, library with files)
- `runner-03-create-library.png`  (optional)
- `runner-04-unlock-sheet.png`    (optional)

Optional/⚠ shots: skip if hard to trigger — the written spec is enough; these
only calibrate visual feel.

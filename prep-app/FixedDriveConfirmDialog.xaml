<Window x:Class="FreeAiSsd.PrepApp.FixedDriveConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fixed drive warning"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="{StaticResource BgBaseBrush}">
    <Grid Margin="24" MinWidth="480">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="WarningText" Style="{StaticResource BodyText}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <CheckBox x:Name="UnderstandCheck" Grid.Row="1"
                  Content="I understand the risk and want to continue"
                  Foreground="{StaticResource StatusWarningBrush}"
                  Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource GhostSecondaryButton}" MinWidth="100" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Proceed" Style="{StaticResource TactilePrimaryButton}" MinWidth="100"
                    IsEnabled="{Binding IsChecked, ElementName=UnderstandCheck}"
                    Click="Proceed_Click"/>
        </StackPanel>
    </Grid>
</Window>

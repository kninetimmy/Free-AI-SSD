# Project State

Last updated: 2026-05-11 (latest) (PR #283 M18 + M19 Mac PrepApp fixes merged `a9d5ac7` — closes the last two actionable items from the Codex multi-PR review of #264–#274. M18 (GH #279 MEDIUM): Manage Models "Add a model" disclosure was `.disabled(!vm.canManageModelsAdd)` on encrypted drives, hiding the inner C7 explanation behind a chevron the user could never expand. Disclosure is now always expandable; C7 hint surfaces on the label itself; picker stays hidden (not just disabled) to avoid running its Refresh / source-switch side effects on a drive where pulls can't run. M19 (GH #280 MEDIUM): `set-hf-token` / `search-hf` payloads hand-rolled JSON with `\\` + `\"` escaping only; raw newlines/tabs/control chars passed through unescaped and produced malformed JSON the sidecar's `JsonDocument.Parse` rejects. New `CommandPayloadEncoder.encode([String: String])` routes both call sites through `JSONSerialization`. 83/83 Swift tests pass (6 new M19 pins covering newline+tab+CR regression class); 931 .NET tests pass; pre-existing 4-failure set unchanged.)

Last released: **v1.3.24** (2026-05-11). **v1.3.25 dispatch in flight** (bundles PRs #272 + #274 + #275); **PR #281 (M16 + M17) and PR #283 (M18 + M19) will roll into the next dispatch.**

> v1.2.7 tag exists on `af77abc` but has no GH release artifact; v1.2.8 supersedes it.

**Currently between tasks.** Most recent: PR #283 closed M18 + M19,
the last two actionable items from the Codex multi-PR review of
#264–#274 (Mac PrepApp surface only — no sidecar change). The
review batch is now fully cleared. **Natural next pick: M15 OR C7**
— both still warm from prior sessions, both Mac-leaning, both
unblock follow-on UX. **M15** (Mac PrepApp: don't auto-advance past
pull failure — surface inline so user can read the error) addresses
the same observability gap that hid the HF manifest bug for so long
and sits in the same M18+M19 spirit (Mac PrepApp UX bugfix).
**C7** (encrypted config persistence — was X9) directly unblocks
the Manage-models Add/Remove gates M18's hint specifically calls
out; once C7 ships, the C7 explainer caption M18 just added can be
removed and the gates can flip live. Either is a valid next pick;
M15 is the lighter-weight one.

Back-burnered: **M2** (signing + notarization, awaiting Apple
Developer cert renewal), **M10** (Mac PrepApp finalize
observability, cold-load wait acceptable post-MAC39).
Ancestor-walk consolidation into `prep-core/BundleContentRoots`
remains overdue.

## Recently shipped

- **PR #283 — M18 + M19: Mac PrepApp Codex review follow-ups — merged `a9d5ac7` (2026-05-11).** Will roll into the next dispatch. Closes the last two actionable items from the Codex multi-PR review of #264–#274 (findings #3 + #4, both MEDIUM). Both Mac-only, SwiftUI / `mac-prep-app` only — no sidecar change, no Windows mirror needed. **M18 (GH #279):** `ManageModelsStepView.swift` Add disclosure was `.disabled(!vm.canManageModelsAdd)` on encrypted drives, hiding the C7 explanation behind a chevron the user could never expand. Disclosure is now always expandable; the C7 hint surfaces on the label itself (visible without expanding); the longer message stays inside the content. Picker is hidden — not just disabled — to avoid running its Refresh / source-switch / HF-token side effects on a drive where pulls can't run. **M19 (GH #280):** new `mac-prep-app/Sources/CommandPayloadEncoder.swift` routes `set-hf-token` and `search-hf` payloads through `JSONSerialization` instead of hand-concatenated `\\` + `\"` escaping. The old path emitted raw newlines / tabs / control chars unescaped, producing malformed JSON the sidecar's `JsonDocument.Parse` rejects with a non-obvious "payload parse failed". Encoder is a free-standing utility (no SwiftUI deps) so the test runner can include it without pulling in `PrepViewModel`. 6 new Swift test pins (encoder accepts ASCII / embedded quotes / backslashes / newline+tab+CR; emits single-line JSON; round-trips through `JSONSerialization`); 83/83 Swift tests pass; 931 .NET tests pass; pre-existing 4-failure set unchanged. CI green first run all three jobs (mac-prep 1m6s, mac-runner 48s, windows 3m4s).

- **PR #281 — M16 + M17: Mac sidecar remove-model guard + locked stdout writer — merged `38ff406` (2026-05-11).** Will roll into the next dispatch. Two fixes from a Codex multi-PR review of #264–#274. **M16 (HIGH, GH #277):** `HostLifetime.cs:608` guard checked `_ollamaServer is not null` to refuse remove-model during in-flight pulls, but `_ollamaServer` is started lazily on first pull and reused for the sidecar lifetime by design (MAC27 + MAC35). After any successful pull, every subsequent Remove was permanently refused with a misleading "wait for the pull batch to complete" message that waiting could never resolve. Fix: check `_activePullCts` under `_pullCtsLock` (the real in-flight signal) and dispose the idle staging-pinned server before starting the SSD-pinned temp server so port 11434 is free. **M17 (MEDIUM, GH #278):** PR #272's pre-pull exception fallback in `Program.cs:155` wrote `result: pull-model …` directly to stdout via `WriteLineAsync`, bypassing `_stdoutLock` that every other sidecar write uses. Because pull-model runs detached on `Task.Run`, the command loop can emit `progress:` / `result:` frames concurrently — a torn line breaks Swift's parser and reintroduces the exact hang #272 was meant to prevent. Fix: new `HostLifetime.EmitFailureResult` routes the fallback through the locked `WriteLineSafe` path. 3 new test pins (post-completion remove success + in-flight pull still refuses cleanly + 32-writer concurrency probe on `EmitFailureResult`). 931 .NET tests pass; pre-existing 4-failure set unchanged. CI green first run all three jobs (mac-prep 56s, mac-runner 51s, windows 4m44s). **Cross-OS audit:** WPF runner has no equivalent staging-pinned shared server and no detached stdin command loop; no Windows mirror change required.

- **PR #275 — C27 follow-up: fix Hugging Face pull failing after 100% download — merged `0a4a2e5` (2026-05-11).** Will roll into the next dispatch. Diagnosed via the user's on-disk sidecar log: every HF pull went `progress: success` → `Pull failed: Unable to locate model blob …`. Root cause: `ModelOperations.FindModelBlobForModel`, `EstimatePartialProgress`, `DiscoverModelsOnDisk`, and `OllamaModelStager.MergeToSsdAsync` all hardcoded `manifests/registry.ollama.ai/library/{name}/{tag}` paths, but Ollama writes HF manifests under `manifests/hf.co/<owner>/<repo>/<quant>`. PR #272's `pull-exception` catch made the failure visible without realizing every HF pull was tripping it — **no HF pull had ever worked** regardless of token. Fix: new shared `ModelOperations.TryResolveOllamaManifestPath` resolver with built-in segment safety (`[A-Za-z0-9._-]`, no `..`, no separators; HF tags exactly two segments after `hf.co/`). All four consumers route through it. `DiscoverModelsOnDisk` now reconstructs full `hf.co/owner/repo:quant` instead of stripping the prefix (picker reconciliation pin). Removed duplicated dead `IsSafeModelTag` from both `ModelOperations.cs` and `OllamaModelStager.cs`. 6 new test pins (5 in `ModelOperationsTests` + 1 in `OllamaModelStagerTests` + helper); existing hostile-tag refusal pin still passes via the resolver's segment safety. 929 .NET tests pass; pre-existing 4-failure set unchanged. CI green first run all three jobs. **Architectural decision pinned in `project_decisions.md`** under 2026-05-11.

> Older entries (PR #274 C6 detect-configured-drive flow, PR #272 C27 HF follow-up, PR #270 C27 Stage 3+4, PR #268 C27 Stage 2, PR #266 C27 Stage 1, PR #173–#264 MAC1–MAC40 + F2a v1.3.22 + M11/C1/M12/M13/C2/M14 + C3+C4+C5 + C24 + C25+C26 — all merged 2026-05-05 through 2026-05-11) pruned to keep this section bounded. Full descriptions live in `git log` and the per-item entries in `agent_docs/mac_project_backlog.md` + `agent_docs/project_decisions.md`.

## Next up

**Authoritative priority lives in `project_backlog.md`** under the 2026-05-10 P0–P5 ranking. Highlights:

1. **P0 field-test bug surface — cleared.** M11, C1, M12, M13, C2, M14 all merged this session-cluster.
2. **P1 F2a model-picker cluster — cleared** (C3+C4+C5 bundled in PR #259 `9f81bd5`).
3. **P1.5 picker filter cluster follow-ons — cleared.** C24/C25/C26 + C27 Stages 1–4 + PR #272 follow-up bug fixes all closed.
4. **P2 substantive UX — cleared.** **C6 done PR #274 (`93a677d`) 2026-05-12.**
5. **P3 critical pre-existing:** `C7` (was X9, encrypted config persistence — **directly unblocks the Manage-models Add/Remove gates C6 disabled per D13**), `C8` (was X10, document replacement consistency), `W1` (was X11, companion keyboard PTT), `C9` (was X12, download verify-before-move), `C10` (was F4 Stage 2, post-setup launch flow), `W2` (was F4 Stages 3-4, companion installer), `C11` (was B2, LAN discovery), `C12` (was R1 Stage 2, runner-cli slash-commands), `M7` (was MAC20, cross-platform release ZIP layout rework).
6. **P4 RAG audit batch** (slot when v1.3.x feature work resumes): `C13` (X18), `C14` (X15), `C15` (X19), `C16` (X20), `C17` (X22), `C18` (X23), `C19` (X17).
7. **Back-burnered per user:** `M2` (was MAC11, signing + notarization, awaiting Apple Developer cert renewal), `M10` (was MAC37, finalize observability, cold-load wait acceptable post-MAC39).
8. **Cleanup follow-up (now overdue):** fold `MacArtifactAvailability.EnumerateContentRoots`, `ArtifactStagingService.EnumerateBundledContentRoots`, and `PrereqService.EnumerateBundleRoots` into one shared `prep-core/BundleContentRoots` helper. All three are byte-identical ancestor-walk enumerators.

**RAG audit backlog:** X17-X23 cover audit findings; X10/X13/X15 scope expansions recorded. Plan: `C:\Users\Kninetimmy\.claude\plans\okay-i-want-to-glowing-galaxy.md`. v1.3.x sequence: X18 -> X15 (expanded) -> X19 -> X20 -> X22 -> X23. X17 reduced to Stage 1 textless-page diagnostic (full OCR deferred — workload is text-layer PDFs).

**Dormant (could not reproduce):** X1-Redux. Diag branch `diag/x1-redux-send-hang` stays on remote, unmerged.

See `project_backlog.md` for full general backlog details. See
`agent_docs/mac_project_backlog.md` for the macOS support track.

## Last session

2026-05-11 (M18 + M19 Mac PrepApp fixes — closes the Codex review batch) (PR #283 merged `a9d5ac7`). Same-day continuation of PR #281's M16 + M17 work: knocked out the two remaining actionable items from the Codex multi-PR review. **M18 fix:** removed `.disabled(!vm.canManageModelsAdd)` from the `ManageModelsStepView` Add disclosure; restructured the label to a `VStack` that shows "Add a model" plus an inline C7 hint caption when read-only (so the user sees the explanation without needing to expand); kept the picker hidden in the disabled branch (not just disabled) to avoid running its Refresh / source-switch side effects on a drive where pulls can't run. The longer C7 explanation still lives inside the disclosure content for users who expand. **M19 fix:** new `mac-prep-app/Sources/CommandPayloadEncoder.swift` exposes `encode([String: String]) -> String?` backed by `JSONSerialization.data(withJSONObject:)`; both `pushHuggingFaceTokenToSidecar` and `refreshHuggingFaceCatalog`'s search path route through it. Kept as a free-standing utility (no SwiftUI deps) so the test runner can include it without dragging in `PrepViewModel`. Wired the new file into both `swiftc` invocations in `.github/workflows/build.yml` (test compile + app compile) and the test-file doc-comment "How to run" example. **Tests:** 6 new Swift test pins in `PrepAppTests.swift` — ASCII / embedded quotes / embedded backslashes / newline+tab+CR (the regression class) / single-line invariant / round-trip JSON-object structural validity; each uses a private `parseSinglePairJSON` helper so assertions don't depend on JSON key order. 83/83 Swift tests pass (77 existing + 6 new); 931 .NET tests pass, pre-existing 4-failure set unchanged (the rule says run the .NET suite as a no-regression check even though .NET surface didn't change). CI green first run all three jobs (mac-prep 1m6s, mac-runner 48s, windows 3m4s). **Cross-OS audit:** both fixes are `mac-prep-app/Sources/*.swift` only — no `prep-core/` change, no sidecar change, no Windows mirror needed. **No surprises** — both fixes matched the prescribed gemini-code-assist / Codex suggestions verbatim; the only judgment call was hiding the picker (vs. rendering it disabled) when `!canManageModelsAdd`, taken because the picker has its own VM-mutating side effects (Refresh button, source switch, HF token field) that have no business firing on an encrypted drive.

2026-05-11 (M16 + M17 Mac sidecar fixes from Codex multi-PR review) (PR #281 merged `38ff406`). User pointed me at `~/Desktop/free-ai-ssd-last-11-pr-code-review.md` — a Codex review of PRs #264–#274 — to vet the findings. Verified all 7 findings against current source (`HostLifetime.cs:608`, `Program.cs:155`, `ManageModelsStepView.swift`, `PrepViewModel.swift`); confirmed Codex's severity ranking; opened GH issues #277–#280 for the four actionable items; priced the remaining 3 as low / backlog. **Plan:** start with the first two (#277 HIGH + #278 MEDIUM, both Mac sidecar) and bundle as one PR; defer #279 + #280 (Swift-only) to a second PR after. **Labeled per the unified scheme:** new mapping table rows M16 (GH #277), M17 (GH #278), M18 (GH #279), M19 (GH #280); full bodies for M16 + M17 added to `mac_project_backlog.md` following the M15 format; M18 + M19 bodies will be added when picked up for work. **M16 fix:** changed guard to check `_activePullCts` under `_pullCtsLock`; added idle-server-dispose path before the SSD-pinned temp server starts. Flipped one existing test from refusal-after-pull to success-after-pull; added a new TaskCompletionSource-gated in-flight test that parks the pull inside `PullModelAsync` so remove lands while `_activePullCts` is set. **M17 fix:** new internal `HostLifetime.EmitFailureResult` routes `Program.cs`'s fallback through the locked `WriteLineSafe` path. New 32-writer × 8-emit concurrency probe asserts every output line is a whole, parseable `result: pull-model {...}` frame. **Cross-OS audit:** WPF runner has no equivalent staging-pinned shared server and no detached stdin command loop; no Windows mirror change required. **Tests:** +2 net (M16 flips one + adds one; M17 adds one); 931 .NET tests pass, pre-existing 4-failure set unchanged. CI green first run all three jobs.

> Older entries (HF manifest path fix PR #275, C6 session PR #274, C27 HF follow-up PR #272, C27 Stage 3+4 PR #270, C27 Stage 2 PR #268, C27 Stage 1 PR #266, C25+C26, C24, C3+C4+C5, M14, M11, C1, M12, M13, C2 — all 2026-05-05 through 2026-05-11 merges) pruned to keep this section to the 2 most recent dated entries per the wrap-up rule. Full descriptions live in `git log` and the PR bodies.

## Open questions

- **PR #283 M18 + M19 field-test pin (new — awaiting next dispatch, Mac only).** **M18:** plug a previously-prepped + encrypted SSD → DriveSelection banner reads "This SSD is already prepared." → Manage models → "Add a model" disclosure renders the inline "Unlock required (C7) — disabled on encrypted drives." caption next to the label (visible without expanding). Click the chevron → the longer C7 explanation expands; the picker does NOT render in the expanded content. Click Done → returns to drive selection cleanly. **M18 unencrypted regression:** previously-prepped unencrypted SSD → Manage models → "Add a model" disclosure has no C7 caption next to the label; expanding shows the full `StarterModelPickerView` + `Pull selected` button (the unchanged path). **M19:** paste a HF token containing a trailing newline (copy with `\n` from a terminal) into the inline token field → sidecar accepts without `payload parse failed` in `<SSD>/logs/macos-prep-host-YYYYMMDD.log`. HF search with an embedded quote or backslash in the query → returns results normally (regression pin for the previously-handled cases). Stress: paste a multi-line token (e.g. accidental two-token paste) → sidecar still accepts a single well-formed JSON line; no torn output.

- **PR #281 M16 + M17 field-test pin (new — awaiting next dispatch, Mac only).** **M16:** Mac PrepApp Manage Models → pull a model from the Add disclosure → wait for completion → returned to `.manageModels` → click Remove on any installed model (including a different model than the one just pulled) → row drops; `<SSD>/logs/macos-prep-host-YYYYMMDD.log` shows `Disposing idle staging-pinned Ollama server before remove (frees port 11434).` followed by `Deleting <tag> from <ssd>/models via ollama rm…`. No `pull-in-flight` refusal. **M16 in-flight contract still holds:** start a pull, while it's still running click Remove on another model → sidecar emits `remove-model ok=false reason=pull-in-flight` (the C6 pin still passes). **M17:** trigger a pre-pull exception path (e.g. delete the staged Ollama exe between `ensure-structure` and Pull selected, or wedge the disk-full precheck) under concurrent activity → UI advances past the failure without hanging; sidecar log shows a single well-formed `result: pull-model {"ok":false,"reason":"pull-exception",...}` line with no interleaved characters from concurrent `progress:` writes.

- **PR #275 HF manifest path fix field-test pin (new — awaiting next dispatch, both OSes).** **Mac priority (original symptom):** with a valid HF token, pull `hf.co/Qwen/Qwen3-8B-GGUF:Q4_K_M` (or any HF GGUF). Expected: NDJSON ends with `progress: success`; **no** `Pull failed: Unable to locate model blob …` line in `<SSD>/logs/macos-prep-host-YYYYMMDD.log`; new `Merge complete: hf.co/Qwen/Qwen3-8B-GGUF:Q4_K_M now resolvable on SSD.` line; sidecar emits `pull-model ok=true`; UI advances to readiness with the model marked installed under its full `hf.co/Qwen/Qwen3-8B-GGUF:Q4_K_M` tag (not the stripped `Qwen3-8B-GGUF:Q4_K_M`); SSD has `<SSD>/models/manifests/hf.co/Qwen/Qwen3-8B-GGUF/Q4_K_M`. **Non-HF regression pin:** pull a stock ollama.com tag (e.g. `llama3.2:1b`) on the same flow — manifest still lands at `<SSD>/models/manifests/registry.ollama.ai/library/llama3.2/1b`. **Windows parity:** same flow on WPF Models tab → works since `prep-core/ModelOperations.cs` is shared.

- **PR #274 C6 field-test pin (new — awaiting first run on a v1.3.25+ build, both OSes).** **Windows priority:** plug a previously-prepped SSD → cyan banner above the Drive-preparation GroupBox reads "This SSD is already prepared. N model(s) on this drive." with [Manage models] + [Start over (formats drive)] buttons. Format/Volume label/prep targets controls below disabled. Click Manage models → switches to Models tab; no FTUE spotlight. Click Start over → C6 pre-confirm modal "Erase already-prepared SSD?" → existing ConfirmErase → UAC → format. **ConfiguredEmpty variant:** prep drive but manually delete `models/manifests/` → banner reads "This SSD is prepared but has no models yet."; both buttons surface. **Encrypted variant:** existing encrypted-drive warning strip surfaces; mutation gates fire as today. **Unconfigured no-regression:** fresh SSD shows no banner, Format + FTUE work as before. **Foreign-data guard:** SSD with manifests but no `portable-config.json` shows no banner; standard Format flow proceeds. **Mac priority:** banner with cyan accent above candidate list; Form + Continue disabled while banner visible. Manage models → `.manageModels` step → sidecar log shows `ready`, `SSD layout verified.`, catalog status update, `discover-models` populates list (NO `stage-runner / stage-ollama / stage-prereqs` log lines — light-touch startup verified). Per-row Remove → NSAlert pre-confirm → `Deleting <tag> from <ssd>/models via ollama rm…` log → row drops. Add disclosure → picker matches prep flow; `Pull selected` transitions to `.modelPull`, on completion returns to `.manageModels` with new model in list (NOT `.readiness`). Done → `.driveSelection` + sidecar shutdown. **Encrypted Mac variant:** yellow banner explains C7; installed list visible; Add disclosure disabled; per-row Remove buttons disabled. **Start over (Mac):** `.warning` NSAlert → `.eraseConfirmation` → `.critical` NSAlert → format. **Selection switching:** configured / fresh / configured drives toggle banner correctly (confirms `selectedCandidate.didSet` fires `refreshDriveConfigurationState`). **Pull-in-flight remove gate pin:** start a pull batch, while it's still running click Remove on another model → sidecar emits `remove-model ok=false reason=pull-in-flight`; UI shows "Remove unavailable while a model pull is running. Wait for the pull to complete and try again."

- **PR #272 follow-up field-test pin (new — awaiting v1.3.25+ build, both OSes; bundles the five HF bugs).** **Mac PrepApp:** Source = Hugging Face → click `thinking` chip → rows stay full opacity and remain checkable (not faded); same for `audio`. **Bundled-catalog cap pin:** Source = Ollama, BEFORE clicking Refresh → set Max size to ≤7B → phi3:14b / qwen2.5:14b / codellama:13b disappear; only 7B-and-under stay. **Parent-row disable pin:** Source = HF → checkbox on `hf.co/mixedbread-ai/mxbai-embed-large-v1` (parent) is greyed; clicking the row label expands the chevron instead; check a quant child (`:F16`) → row checks normally. **Token gate pin:** with one HF child checked and the token field empty, click "Continue without encryption" → explainer modal pops with 5-step instructions; clicking "Open Hugging Face" launches `https://huggingface.co/settings/tokens` in the default browser. Paste a token → Continue advances. **Windows runner Models tab parity:** same gates on `DownloadCommand` — parent toggle disabled, Confirm dialog on Download with HF row + no token. **HF API filter pin:** Source = HF with no search → top results match huggingface.co/models?apps=ollama exactly (mxbai-embed-large-v1 4.4M, then unsloth/gemma-4-26B-A4B-it-GGUF 3.4M, etc); no `text-to-video`/`text-to-image` repos in the list. **Bug-5 hang pin (Mac):** pick an HF quant that Ollama refuses with 400 (field repro: `unsloth/Qwen3.5-9B-GGUF:IQ2_M`); after 100% download status flips to a clean `Pull failed for hf.co/…:IQ2_M: …. Try a different quant (Q4_K_M is the broadest-compatible)…`; UI advances past the pull step (no infinite spinner). **Bug-5 sibling-fetch pin:** during any HF quant pull the SSD log must NOT contain `Could not fetch Hugging Face siblings for owner/repo:QUANT (NonSuccessStatus: 404)` — the bare repoId is now passed.

- **v1.3.24 field-test pin (awaiting first run, both OSes; bundles C3/C4/C5/C24/C25/C26/C27 Stages 1–4):** **C3/C4/C5/C24 (Mac priority — F2a follow-ons reported here originally):** PrepApp picker → Refresh from Ollama → set Max size to ≤14B; rows above 14B disappear, configured + on-disk + bundled rows stay visible (null-params pass-through). Caption reads `Showing N of 399 matching filter (≤14B).` Click `tools` chip → capability AND filter narrows to tool-capable models only. Pick Sort: Newest / A–Z. **C25 marker:** chip engaged → rows with empty Capabilities render at opacity 0.55 with tooltip — **note:** per PR #272, HF rows are excluded from this fade trigger; only Ollama-source rows fade. **C26 dropdown:** Most-popular limit Top 10/15/25/50. **C27 Stages 1–2:** Source dropdown → Hugging Face → ~50 popular GGUF rows. Type "qwen" → debounced (350ms) `search-hf` fires. Pull `Qwen/Qwen3-8B-GGUF` → temporary Ollama server starts, NDJSON renders `Pulling hf.co/Qwen/Qwen3-8B-GGUF…`, completes. Pull > free disk → confirm-dialog (WPF) / staging-precheck refusal (Mac) names filename + GB + headroom callout. Multi-part repo → warning reports `(N-part split)` + summed size. **C27 Stage 3 (token auth):** Source: Hugging Face → "HF token (optional)" `PasswordBox` (Windows) / `SecureField` (Mac) appears next to the dropdown. Paste a token → no log leak; switch back to Ollama → field hides. With encryption OFF + non-empty token → yellow banner: `⚠ Encryption is off — your Hugging Face token will be stored in plaintext on the SSD…`. Pull a known gated GGUF without a token → friendly refusal pointing at the inline token field. Pull a gated GGUF with a valid token → succeeds (Ollama server inherits `HF_TOKEN` env). Finalize SSD with encryption ON + token → re-prep / unlock recovers the token from the sealed config. **C27 Stage 4 (lazy quant rows):** Click the chevron on an HF parent row → `…` placeholder briefly → quants expand below parent in size-ascending order (`IQ` < `Q2` < `Q3` < `Q4` < `Q5` < `Q8` < `BF16`/`F16` < `F32`). Each child row shows the quant label + size (e.g. `Q4_K_M ≈ 4.5 GB`). Multi-part series (e.g. `Qwen3-70B-Q4_K_M-00001-of-00003.gguf`) → single child row labeled `Q4_K_M (3-part split)` with summed size. Pull a child row → `hf.co/Qwen/Qwen3-8B-GGUF:Q4_K_M` flows through Ollama unchanged. Collapse → re-expand → no API call (children cached). Switch source back to Ollama → chevron + children disappear. **PR #272 update:** parent rows are now non-checkable — clicking the parent row instead toggles the chevron expansion; only quant children can be pulled.

- **M14 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Mac Runner → unlock SSD → wait for `networkApiBaseUrl` to populate → Documents section → click "Pull embedding model". `libraryStatus` reads `Pulling embedding model…` then `Embedding model ready: nomic-embed-text:latest`. Force-quit Ollama via the runner's Stop control and click again → status surfaces the 503 reason (`Ollama host is not running. Start Ollama before pulling the embedding model.`). Companion-app parity: a Companion client with the same Bearer key can `POST /api/models/embedding/pull` and receive the same 200/503 shape. WPF runner unchanged → confirm by clicking the existing "Pull embedding model" button in the Index tools expander; behavior identical to pre-M14.

- **M11 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** PrepApp picker → Refresh from Ollama → click Most-popular ON. New accent-cyan line appears between the catalog status text and the picker reading `Showing top 15 of 399 by pulls.` (or whatever the live total is). Click OFF → line disappears. Type `llama` in search → reads `Showing N of 399 matching search.` Combine search + Most-popular → reads `Showing top N of 399 by pulls (filtered by search).` **Windows parity:** Models tab → Refresh → toggle Most popular. Same caption strings appear above the merged Models grid; configured + on-disk rows still surface (the top-15 cap only applies to Recommended-source rows, but the caption counts the entire visible ModelRows set). **Empty-catalog edge:** pre-Refresh on a freshly-prepped drive → caption stays empty under both toggle states (the existing "Refresh first" empty-state branch handles UX guidance).

- **C1 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** load `qwen3:14b` cold (force-quit Ollama first or pick a freshly-prepped SSD), Send → status reads `Loading qwen3:14b… 20s` then `…40s` then `…60s` etc. through the cold-load duration; flips to `Generating…` once the first token arrives; response area fills normally; final status `Answered` or `Answered with sources`. **SSD log evidence:** `chat stream begin (model=qwen3:14b, host=…)`, then a long gap, then `chat first-token in NNNNms (model=qwen3:14b)` and `chat stream complete in NNNNms`. **Warm-load contract:** second Send within 5 min of first → no `loading` frames in NDJSON, status goes `Sending... → Generating… → Answered`. **Windows parity:** same model + cold-load on Windows runner → `StreamingIndicator` reads `● Loading model… NNs` until tokens flow; PTT chat (which uses streaming under the hood) does NOT paint the loading text in the chat tab (visibility gate suppresses). **Stress pin:** if heartbeats keep flowing past 180s and no token ever arrives, that's the residual phase-1 case — Ollama-side load issue (memory pressure / corrupted blob); user can hit Stop and report. Heartbeat timer + write-gate prevent the silent-timeout class of bugs from ever recurring; this pin verifies the behavior end-to-end.

- **M12 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Send with empty model selected → red banner reads `"Model is required."` (validation 400 body). Force-quit `mac-runner-host` mid-request → red banner reads the URLSession transport reason (e.g., `"The network connection was lost."`); status strip also reads `Chat failed: ...`. Trigger a `RagRetrievalFailed` (e.g., temporarily delete the embedder) → orange `ragWarning` banner shows the RAG reason, response area shows the model's answer-without-context, no red banner. Mid-stream backend failure (e.g., kill ollama mid-stream) → red banner shows the `error` NDJSON frame message; file log gains `Chat backend error: <msg>`. Library ops with sidecar down → `libraryStatus` shows the bare reason (e.g., `"Service unavailable."`) without the now-removed `"Chat failed: "` prefix.

- **M13 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Click "Expose API on LAN" toggle ON → toggle stays ON; runner log shows `API host bound to <configured-bind>` (not `127.0.0.1`); `networkApiStatus` reads `API: <baseUrl>` with the LAN address. Click toggle OFF → toggle stays OFF; log shows `API host bound to 127.0.0.1`. Toggle ON, then force-quit the sidecar process from Activity Monitor → toggle reverts to OFF with `networkApiStatus` showing the crash message (proving `.crashed` path still clears intent). Lock + Unlock with toggle persisted ON in config → toggle hydrates ON and stays ON after auto-restart.

- **PR #247 C2 field-test pin (new — awaiting first run on a fresh-prepped Mac SSD):** drop a sub-1MB PDF into a new library → ingest succeeds end-to-end without manual embed-model setup. Re-finalize a fully-prepped drive → log shows `Embedding model nomic-embed-text:latest already on disk; skipping pull` and no temp Ollama server is spun up. Failure-message pin: temporarily delete the embedder from the SSD, re-ingest → error names `model 'nomic-embed-text' not found` rather than just the threshold ratio.

- **v1.3.22 mac + windows field-test pin (F2a) — released 2026-05-10 same session; awaiting first field run.** **Mac side now also covered by the M11 pin above** — the field-test confirms both the underlying filter behavior AND the new caption that announces the cap. **Mac picker smoke (priority):** PrepApp picker fills the page; resizing the window grows the list; search filters live as user types; clicking "Most popular" before Refresh shows the empty state with hint to Refresh first; after Refresh, Most popular shows ~15 entries sorted by pull count desc + the M11 caption announces the cap; clearing search/popular returns the full list and the caption hides. **Windows picker smoke:** Models tab gets the new search + Most popular row + M11 caption; same filter behavior; configured + on-disk rows stay visible even with Most popular on.
- **MAC37 driver pin (still applicable but back-burnered):** measure how long Mac PrepApp finalize takes on a fresh pull; observe whether it's actually doing work or waiting on a timeout. Not blocking — user accepted the 20s post-cache wait.
- **MAC35a/MAC34b/MAC35 deferred field-test smokes (still applicable on v1.3.22):** AppleDouble sidecar regression — re-running readiness on a v1.3.15-prepped SSD passes all-green; port 11434 reclaim — Ollama.app already running → Runner.app log shows "Found N existing process(es) on port 11434 (PIDs ...)"; pull cancel/resume scenarios — partials persist, no torn blobs after re-run; disk-full precheck fires when staging volume has < 2× model size free.
- **F2 manual smoke (partially complete on v1.3.5 mac field test):** Mac side confirmed at v1.3.5 + F2a at v1.3.22. **Windows side still deferred** to a Windows machine: online → click Refresh → grid repopulates; offline → caption explains failure, bundled list stays in place.
- **MAC20 still pending:** bundle root scatters Windows DLLs alongside `payload/` until MAC20 reshapes to `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE`.
- Before a public signed Mac beta, verify whether the nested `payload/mac/Runner.app.zip` (or whatever path MAC20 lands on) preserves the stapled notarization ticket after users download and extract the cross-platform ZIP. If not, ship a standalone notarized app ZIP or DMG.
- **MAC6 manual smoke (deferred until a Mac on the same LAN as a Windows Companion):** Windows Companion discovers and connects to a Mac-hosted Runner with Bearer auth + /api/health + /api/chat; Network Mode toggle with real Ollama serving a real model end-to-end.
- **MAC8 manual smoke (deferred to a real Mac):** Network Mode on → Documents UI populates → Create library → Add Files (TXT) → ingest completes → Send chat → returns sources from the uploaded file.
- **MAC10b manual visual smoke (deferred to a real Mac + a Windows machine):** Runner.app shows the Free-AI-SSD icon in Finder, the Dock, and command-tab on a clean Mac; WPF Runner / PrepApp / Companion show the icon in title bars and the Windows taskbar.
- **MAC17 manual smoke (deferred to a real Mac + an external SSD):** plug an external SSD → launch `PrepApp.app` → drive shows in selection list → choose Mac-only or cross-platform target → format → SSD layout → Mac Runner staged → Mac Ollama staged → starter model pulls → encryption setup with passphrase → readiness all green → finalize. Plus the cross-platform roundtrip.

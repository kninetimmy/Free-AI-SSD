# Project State

Last updated: 2026-05-09 evening (PR #229 MAC35 merged `0eecceb` — Mac model pulls stage to host APFS then merge sequentially to the SSD. v1.3.15 dispatched + shipped same session — bundles MAC34b + MAC35. Awaiting Mac field test on exFAT against the v1.3.14 baseline.)

Last released: **v1.3.15** (2026-05-09; MAC34b + MAC35 cumulative bundle — `lsof`-driven port 11434 reclaim + Mac host-stage pull then merge. `Free-AI-SSD-win.zip` 335 MB, `Free-AI-SSD-beta-crossplatform.zip` 644 MB shipped).

> v1.2.7 tag exists on `af77abc` but has no GH release artifact; v1.2.8 supersedes it.

**MAC35 closes the v1.3.14 mac field-test throughput collapse
pulling `qwen2.5:7b` (4.7 GB on a 1 Gb line, ~5 MB/s, 290 stalls
over 19 min):** Ollama 0.5.7 hardcodes `numDownloadParts = 16` and
exFAT FSKit cannot sustain 16 concurrent writers. Mac pulls now
stage into `~/Library/Caches/FreeAiSsd/ollama-staging` (host APFS)
and merge sequentially to the SSD; Windows path unchanged because
NTFS handles the same I/O pattern. **MAC30** (encryption opt-in,
default OFF) now leads "Next up" — the encryption decision is
locked but plaintext codepath restoration on both PrepApps is
still pending. **MAC20** (cross-platform ZIP layout rework),
**F2a** (picker sort + resize gaps), and **X18** (ingest
observability) remain queued. **MAC11** (signing + notarization)
remains back-burnered until the user's Apple Developer cert
renews.
The cleanup follow-up to fold `MacArtifactAvailability` /
`ArtifactStagingService` / `PrereqService` ancestor-walk
enumerators into a shared `prep-core/BundleContentRoots` helper
remains overdue.

## Recently shipped

- **PR #229 — MAC35 Mac model pulls stage to host APFS then merge to SSD — merged `0eecceb` (2026-05-09).** Released as **v1.3.15** (cumulative MAC34b + MAC35 bundle). Closes the v1.3.14 field-test throughput collapse: pulling `qwen2.5:7b` (4.7 GB) on a 1 Gb line collapsed to ~5 MB/s on exFAT — Ollama 0.5.7 hardcodes 16 parallel chunk writers (verified upstream — no env override) and exFAT FSKit can't sustain that, triggering false stall-detect → chunk restart → UI bouncing 35-60 % → 6 %. New `prep-core/OllamaModelStager.cs` owns the merge: per-blob atomic tmp-then-rename, size-match skip-if-present, manifest-written-last so a torn merge is invisible to `DiscoverModelsOnDisk`, plus a 2x-model-size disk-space precheck (5 GB floor for unknown sizes via `ModelSizingCatalog.Suggest`). `mac-prep-host/HostLifetime.PullModelAsync` now resolves `~/Library/Caches/FreeAiSsd/ollama-staging`, pins the temp Ollama server's `OLLAMA_MODELS` to staging, then calls `MergeToSsdAsync` under the same pull-CTS so cancel works in both phases. Reuses the staging-side Sha256 for the result envelope — content-addressed copy means SSD blob is byte-identical. **Scope reduction:** runner-side `PullEmbeddingModelAsync` (HTTP `/api/pull` against the long-running Mac daemon) deferred from this PR — restaging it would require restarting the in-process daemon mid-chat; embedding model is ~270 MB so cost of deferral is bounded. **Squash bundling note:** the merge also rolled in the previously-uncommitted MAC34a+34b agent_docs wrap-up content (project_decisions.md, project_state.md) since the wrap-up branch was MAC35's parent. CI required 2 runs (round 1: `Assert.ThrowsAsync<OperationCanceledException>` rejected `TaskCanceledException` subclass — relaxed to `ThrowsAnyAsync`). 10 new stager tests + 4 updated lifecycle pins. v1.3.15 dispatch followed merge in the same session; all four jobs green (mac-prep 1m, mac-runner 56s, windows 4m21s, package-release ~2m); both ZIPs published.

- **PR #226 — MAC34a Mac sidecar handshake hardcodes `networkModeEnabled=true` — merged `95b62b5` (2026-05-09).** Released as **v1.3.14** (MAC34a-only — see note below on MAC34b). Hotfix for the v1.3.13 field-test chat regression: every unlock logged `Mac runner host crashed: RunnerLocalApiService did not start. Ensure networkModeEnabled is true before spawning mac-runner-host.` Root cause: MAC34's Swift contract said the toggle controls bind address only, but `restartHostSidecar` still passed the toggle's value as the `networkModeEnabled` field in the C# handshake. With toggle OFF, `RunnerLocalApiService.StartAsync` early-returned at `if (!config.NetworkModeEnabled) return;`, `HostLifetime.StartAsync` threw on empty `CurrentBaseUrl`, host crashed. Fix hardcodes `true` in the handshake; LAN exposure is now governed purely by `networkBindAddress` (loopback when toggle OFF, configured address when ON). C# inner gate stays as defense-in-depth — Windows pre-gates externally in `MainWindow.xaml.cs:470`. Comment-only update to `HostRunner_WithNetworkModeDisabled_FailsWithoutReadyLine` clarifies its new role. CI green on first run (mac-runner-build 1m1s, mac-prep-build 47s, windows-build 3m25s).

- **PR #227 — MAC34b free port 11434 before launching staged ollama on Mac — merged `d329850` (2026-05-09).** Companion fix to MAC34a; queued for v1.3.15 dispatch alongside MAC35 rather than a standalone v1.3.15 hotfix (user call: bundle MAC34b + MAC35 into one Mac field-test pass once MAC35 lands). v1.3.13 field test surfaced silent `Ollama exited with code 1` because Ollama.app + a stray CLI ollama already held 11434, so the staged binary couldn't bind. New `terminateProcessesListening(onPort:)` static helper in `mac-runner/Sources/main.swift` runs `lsof -nP -t -iTCP:<port> -sTCP:LISTEN` to enumerate PIDs, then SIGTERM → 0.6s grace → SIGKILL anything still alive. Logs `Found N existing process(es) on port 11434 (PIDs ...)` so the user sees what was reclaimed (the user reported "I had two running and had no idea"). Resolves by PID rather than `pkill ollama` so a sibling ollama on a different port stays untouched. Cross-OS parity note: Windows `OllamaLifecycleService.ResolvePort` scans preferred+20 instead — Mac can't port-shift because the C# sidecar handshake takes a fixed host URL. Merged green on first run; not yet field-tested because MAC34b shipped after the v1.3.14 release tag.

- **PR #225 — MAC31a + MAC32 bundled cross-OS PR — merged `179dfc0` (2026-05-09).** Released as **v1.3.13** (`Free-AI-SSD-win.zip` 335 MB, `Free-AI-SSD-beta-crossplatform.zip` 644 MB shipped 2026-05-09; cumulative MAC34 + MAC31a + MAC32 bundle). **MAC31a (Mac):** new `.modelPullPaused(tag, progressSnapshot)` step in `PrepFlowStep.swift`. `pullStarterModels` refactored to capture the in-flight tag + last `pullProgressLine` snapshot on `CancellationError` and transition to the new step instead of falling through to `.readiness` (which buried MAC31's resume seed). New `pendingPullTags` private property persists the cancel→resume queue; `resumePull` re-enters with the cancelled tag first so MAC31's "Resuming `<tag>` from NN%…" log line fires automatically. New `ModelPullPausedStepView` in `main.swift` surfaces Retry / Skip / Start over. Snapshot-from-memory chosen over re-querying the sidecar (cheaper, no roundtrip on Cancel). **MAC31a (Windows):** verified already correct — `CancelOperation` cancels the CTS, `PullModelsAsync` catches and returns to the Models tab where MAC31's seed already populates "Resuming…" on next click. No XAML change. **MAC32 (Mac):** `DoneStepView`'s Finish button silently called `finalize()` (sidecar shutdown + log line) and left the window visibly frozen. Renamed to Quit; new `vm.quit()` shuts down then dispatches `NSApplication.shared.terminate(nil)`. Body copy mirrors the Windows modal. **MAC32 (Windows):** `FinalizeAsync` ended silently — added `_dialogService.ShowInfo("Your SSD is ready. Open Runner.exe on this SSD to start chatting.", "Setup complete")` on the full-success path only. `IDialogService.ShowInfo` already existed (used by `CheckReadinessAsync`). Tests: 4 new Swift cases in `PrepAppTests.swift` pin `.modelPullPaused` Equatable conformance (tag + snapshot round-trip, drift detection, distinct from `.modelPull`/`.readiness`); 3 new Windows cases in `PrepViewModelTests.cs` pin modal-on-success + modal-not-on-no-profile + modal-not-on-readiness-failure. Local validation: `swiftc` mac-prep-tests 23/23 passed; PrepApp.app + Runner.app compile clean against CI's invocation. CI green on first run: `windows-build` 2m55s, `mac-prep-build` 45s, `mac-runner-build` 54s. v1.3.13 dispatch followed merge in the same session; `package-release` 2m52s, all four jobs green.

- **PR #223 — MAC34 Mac Runner local chat works without Network Mode + cross-OS API key generation — merged `e9c9a65` (2026-05-08).** Released as **v1.3.13** (cumulative bundle). Closes the v1.3.12 mac field-test post-prep blocker: prep + pull + unlock all worked, but Send returned `Chat failed: API key is required by configuration but not set on host.` Two stacked root causes addressed in one cross-OS bundle. **(Mac architecture)** Mac chat is architecturally routed through `mac-runner-host` (the C# RAG pipeline lives there) but pre-MAC34 the sidecar only ran when Network Mode was toggled on — so local-only chat looked like it required LAN exposure. Now ollama + sidecar auto-spawn at unlock; the toggle is repurposed as "Expose API on LAN" (runtime bind-address control only — OFF forces 127.0.0.1, ON uses `networkBindAddress`). Start/Stop buttons removed (auto-managed). **(Cross-OS API key gen)** PrepApp shipped `networkApiKey: ""` with `networkRequireApiKey: true`, so the LAN auth gate 503'd every request. Mac `EncryptedConfigWriter.InitialPortableConfigPayload.networkApiKey` defaults to a fresh 32-byte random hex via `SecRandomCopyBytes`; Windows `PrepViewModel.FinalizeAsync` populates `config.NetworkApiKey` if empty via `Convert.ToHexString(RandomNumberGenerator.GetBytes(32))`. Idempotent — re-finalize preserves an existing key. **(Legacy SSD heal)** v1.3.12-prepped SSDs already have empty keys baked in. Mac Runner's `restartHostSidecar` checks `networkApiKey` and runtime-generates one inline if empty, persisting via `saveConfig`. **Considered + reverted "loopback bypass" in `RunnerLocalApiService`** — runtime backfill is cleaner, doesn't change auth posture, and doesn't break the existing `ApiKeyEnforcement_BlocksChatWithoutKey` test. 5 new tests; CI green on first run.

- **PR #221 — MAC31 Pull UX: cancel button, single in-place progress line, resume seed (cross-OS) — merged `c7646f2` (2026-05-08).** Released as **v1.3.12**. Closes the three v1.3.10 mac field-test pull friction points in one bundle. **(a) Cancel button:** Mac PrepApp Models step gains a Cancel button gated on `vm.canCancelPull`; sidecar's `cancel-pull` arm signals `_activePullCts` (linked CTS); `Program.cs` detaches `pull-model` at the loop layer so cancel-while-running works. **(b) Single in-place progress line:** new static `OllamaPullProgressFilter` strips ANSI cursor-rewrite escapes, collapses `\r`-separated rewrites, detects Ollama's `pulling <hash>... NN%` shape. `ModelOperations.Consume` gains optional `onProgress` callback. **(c) Resume seed:** new `ModelOperations.EstimatePartialProgress` sums `<modelsRoot>/blobs/sha256-<digest>-partial-*` against manifest layer totals; pre-pull seed emits *"Resuming `<tag>` from NN%…"*. 18 new tests; CI green on first run.

- **PR #218 — MAC33 Runner reads disk-truth for installed models on Mac-prepped SSDs — merged `bf3a923` (2026-05-08).** Released as **v1.3.11**. Closes the v1.3.10 mac field-test post-prep blocker: prep + readiness all-green, unlock succeeds, but the model selector showed zero options. Three runner-core call sites (`ModelManagementService.GetInstalledModelNames`, `GetModelSizingWarnings`, `RunnerLocalApiService.cs:160` `/models` endpoint) plus the Mac SwiftUI picker (`mac-runner/Sources/main.swift:415` `applyConfigToUi`) all swapped to disk-truth via `ModelOperations.DiscoverModelsOnDisk`. 5 new tests pin disk-truth scenarios. CI green on first run.

> Older entries (PR #173–#215 MAC1–MAC29 + MAC10a/b + MAC16 + MAC17/17a/17b + MAC18–MAC28 — all merged 2026-05-05/06/07/08; F2 live catalog PR #202; MAC21 post-format mount discovery PR #204; MAC22/23/24/25 ancestor-walk + ResolveOllamaExe shaped through PR #211; MAC29 Mac-aware readiness PR #215) pruned to keep this section under the soft 100-line cap. Full descriptions live in `git log` and the per-item entries in `agent_docs/mac_project_backlog.md` + `agent_docs/project_decisions.md`.

## Next up

1. **MAC30** — Encryption optional, default OFF, both OSes. **Product call locked 2026-05-08:** plaintext is allowed; the toggle defaults OFF on both PrepApps; the passphrase prompt becomes a security upgrade, not a gate. Restore the `!enableEncryption` codepath MAC17a deleted, add Swift `PlaintextConfigWriter`, flip both PrepApp defaults. The `NetworkModeEncryptionRequiredMessage` guard at `shared/PortableConfig.cs:275` keeps the API key from ever landing in plaintext. MAC29 + MAC33 + MAC34 + MAC31a + MAC32 + MAC34a + MAC34b + MAC35 all shipped before MAC30 so plaintext-config field testing now has both reliable chat AND reasonable pull throughput. Decision recorded in `project_decisions.md` 2026-05-08; full plan in `mac_project_backlog.md` MAC30.
2. **F2a** — Model picker UX gaps surfaced on the v1.3.5 field test. (1) No sort across the 399-entry catalog (alphabetize / size / tier). (2) Mac `EncryptionSetupStepView`'s ScrollView locked at `.frame(minHeight: 120, maxHeight: 240)` (`mac-prep-app/Sources/main.swift:294`), so the picker stays at 240px when the user resizes the window. Bundle both OSes per the parity rule.
3. **MAC20** — Cross-platform release ZIP layout rework. Filed 2026-05-07 from v1.3.1 field test: root currently scatters loose Windows DLLs + PDBs alongside `payload/`; user wants `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE` at root. Medium; touches CI release-assembly + PrepApp sidecar paths.
4. **X18** — Ingest observability (first item in the v1.3.x RAG audit sequence X18 → X22 → X15 → X19 → X20 → X23). Execution prompt drafted at `agent_docs/x18_execution_prompt.md`. Bundle-cross-OS by default per the strengthened 2026-05-07 dual-OS review pass rule.
5. **MAC11** — Signing + notarization. **Back-burnered** until the user's Apple Developer account renews on payday. MAC10b already landed the bundle metadata so MAC11 is plumbing-ready when the cert returns.
6. **Cleanup follow-up (now overdue):** fold `MacArtifactAvailability.EnumerateContentRoots`, `ArtifactStagingService.EnumerateBundledContentRoots`, and `PrereqService.EnumerateBundleRoots` into one shared `prep-core/BundleContentRoots` helper. All three are byte-identical ancestor-walk enumerators.
7. **X4** unblocked from the host side: only needs SPA assets at `runner-core/wwwroot/chat/`. After F2 demonstrated the cross-OS bundle pattern works cleanly, X4 is a natural next bundled-cross-OS pick.
8. For other non-Mac work, pick from `B2` (LAN discovery — Stage 1 done, Stages 2-4 pending), `F4` (FTUE Stage 2-4), `H3`, or `R1` follow-up (RunnerCli `/docs`+`/reindex` slash-commands).

**RAG audit backlog:** X17-X23 cover audit findings; X10/X13/X15 scope expansions recorded. Plan: `C:\Users\Kninetimmy\.claude\plans\okay-i-want-to-glowing-galaxy.md`. v1.3.x sequence: X18 -> X15 (expanded) -> X19 -> X20 -> X22 -> X23. X17 reduced to Stage 1 textless-page diagnostic (full OCR deferred -- workload is text-layer PDFs).

**Dormant (could not reproduce):** X1-Redux. Diag branch `diag/x1-redux-send-hang` stays on remote, unmerged.

See `project_backlog.md` for full general backlog details. See
`agent_docs/mac_project_backlog.md` for the macOS support track.

## Last session

2026-05-09 evening (PR #229 MAC35 merged `0eecceb` + v1.3.15 dispatched + shipped). New `prep-core/OllamaModelStager.cs` (313 lines) owns the merge contract: per-blob atomic tmp-then-rename (so dest never holds partial bytes), size-match skip-if-present (idempotent retry after cancel), manifest-written-last (torn merge invisible to discovery), 2x-model-size disk-space precheck with 5 GB floor. `mac-prep-host/HostLifetime.PullModelAsync` swap: `_ollamaServer` is now OLLAMA_MODELS-pinned to `~/Library/Caches/FreeAiSsd/ollama-staging` instead of the SSD; merge runs under the same pull-CTS as the pull itself so cancel tears down cleanly in both phases. Internal `HostLifetime` ctor gained a `stagingRootResolver` test seam so lifecycle pins inject a per-test tempdir. Runner-side embedding pull (`PullEmbeddingModelAsync` HTTP `/api/pull`) deferred per the planning AskUserQuestion — restaging the long-running Mac daemon mid-chat is a bigger architectural surface for a 270 MB blob. **CI round 1 caught `Assert.ThrowsAsync<OCE>` rejecting `TaskCanceledException` subclass on Windows** — relaxed to `ThrowsAnyAsync<OCE>` since the pin is on cancel semantics not the precise derived type. **Squash-merge surprise:** GitHub's squash bundled the previously-uncommitted MAC34a+34b agent_docs wrap-up content into the MAC35 commit (the `mac34ab-wrap-up` branch was MAC35's parent), so PR #229 ended up rolling both wrap-up + new feature. Net-positive — the wrap-up was pending anyway. v1.3.15 dispatched same-session via `gh workflow run` (`version=1.3.15 include_macos=true`); all four jobs green (mac-prep 1m, mac-runner 56s, windows 4m21s, package-release ~2m); both ZIPs published to v1.3.15 release.

2026-05-09 later still (v1.3.14 mac field test passed for shipped scope + surfaced MAC35; PR #227 MAC34b merged `d329850`; v1.3.15 deferred until MAC35 lands). v1.3.14 was dispatched and tested on the user's Mac with the SSD as exFAT. The shipped MAC34a fix verified green: chat survives toggle OFF, lock/unlock recovers cleanly, "Expose API on LAN" toggle now controls bind address only. MAC34b items on the v1.3.14 pin (port 11434 reclaim) couldn't be exercised because PR #227 hadn't merged before the v1.3.14 release tag (`95b62b5` = MAC34a-only). **Throughput collapse surfaced** pulling `qwen2.5:7b` (4.7 GB) on a 1 Gb line: 290 stall events over 19 minutes, effective ~5 MB/s, UI bouncing 35-60 % → 6 %. Diagnosis off `<ssdRoot>/logs/macos-prep-host-20260509.log`: `"downloading 2bada8a74506 in 16 292 MB part(s)"` followed by all 16 parts simultaneously stalling — the signature of disk-write contention, not network failure. Verified upstream: Ollama 0.5.7 hardcodes `numDownloadParts = 16`, no env var override exists. Filed as **MAC35** (host-stage pull to APFS then sequential copy to SSD) and promoted ahead of MAC30 in "Next up" because plaintext-config field testing isn't viable at 5 MB/s. PR #227 merged after the diagnosis (CI green: mac-prep 59s, mac-runner 48s, windows 2m53s). User call: bundle MAC34b + MAC35 into a single v1.3.15 dispatch + field-test pass once MAC35 lands, rather than a standalone MAC34b hotfix.

## Open questions

- **v1.3.15 mac field-test pin (MAC34b + MAC35) — dispatched 2026-05-09 evening; awaiting first field run.** **MAC34b smoke:** with Ollama.app already running in the background → launch Runner.app off the SSD → log shows "Found N existing process(es) on port 11434 (PIDs ...)" → bundled ollama binds clean. Sibling-ollama edge: start a second ollama on a non-11434 port → log only mentions the 11434 PID; the sibling is untouched. **MAC35 throughput acceptance:** prep a Mac SSD with `qwen2.5:7b` (4.7 GB) on a 1 Gb line → effective rate ≥ 50 MB/s → completion ≤ 3 min (vs the v1.3.14 baseline of 15+ min / no completion). Re-run pull → "Resuming…" seed fires from staging-side partial files; SSD-side existing blobs short-circuit. Cancel mid-pull → staging partials persist; SSD shows no torn blob. Cancel mid-merge → re-running completes cleanly with no leftover torn blobs. Disk-full precheck surfaces before pull starts when staging volume has < 2× model size free. Windows-side regression check: Windows pull behavior unchanged (still direct-to-SSD).
- **F2 manual smoke (partially complete on v1.3.5 mac field test):** Mac side confirmed — Refresh repopulates picker with live 399-entry catalog; F2a tracks the sort + fill-window UX gaps. **Windows side still deferred** to a Windows machine: online → click Refresh → grid repopulates; offline → caption explains failure, bundled list stays in place.
- **MAC20 still pending:** bundle root scatters Windows DLLs alongside `payload/` until MAC20 reshapes to `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE`.
- Before a public signed Mac beta, verify whether the nested `payload/mac/Runner.app.zip` (or whatever path MAC20 lands on) preserves the stapled notarization ticket after users download and extract the cross-platform ZIP. If not, ship a standalone notarized app ZIP or DMG.
- **MAC6 manual smoke (deferred until a Mac on the same LAN as a Windows Companion):** Windows Companion discovers and connects to a Mac-hosted Runner with Bearer auth + /api/health + /api/chat; Network Mode toggle with real Ollama serving a real model end-to-end.
- **MAC8 manual smoke (deferred to a real Mac):** Network Mode on → Documents UI populates → Create library → Add Files (TXT) → ingest completes → Send chat → returns sources from the uploaded file.
- **MAC10b manual visual smoke (deferred to a real Mac + a Windows machine):** Runner.app shows the Free-AI-SSD icon in Finder, the Dock, and command-tab on a clean Mac; WPF Runner / PrepApp / Companion show the icon in title bars and the Windows taskbar.
- **MAC17 manual smoke (deferred to a real Mac + an external SSD):** plug an external SSD → launch `PrepApp.app` → drive shows in selection list → choose Mac-only or cross-platform target → format → SSD layout → Mac Runner staged → Mac Ollama staged → starter model pulls → encryption setup with passphrase → readiness all green → finalize. Plus the cross-platform roundtrip.

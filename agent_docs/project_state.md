# Project State

Last updated: 2026-05-10 (latest) (PR #259 C3+C4+C5 merged `9f81bd5` — model picker gains parameter-cap dropdown (≤7B/≤14B/≤30B/≤70B), capability AND chips (tools/vision/thinking/audio), and sort dropdown (Popular/Newest/A–Z) on both WPF and Mac. **Scraper extension:** new `x-test-updated` regex + `ParseRelativeDate` ("yesterday"/"N days/weeks/months/years ago" → DateTimeOffset, months/years approximated for ordinal sort), new `ParseParamsBillions` extracting numeric param count from size tokens including MoE notation ("128x17b" → 128 by largest-billion-wins so memory-budget filters exclude MoE). **Projection:** Capabilities/ParametersBillion/LastUpdated added to `StarterModelEntry`, shared `StarterCatalogEntry`, `ModelGridRow`, and Swift `StarterModelEntry`/`StarterModelDisplayEntry`; mac-prep-host payload forwards them; lastUpdated crosses the wire as ISO 8601 string (System.Text.Json default), Swift consumes as String? since ISO 8601 sorts lexically the same as the underlying instants. **Filter logic:** new `IsModelRowVisible` cap+capability checks pass through null/empty data so configured/on-disk rows stay visible; new `ModelSortMode` enum drives WPF `SortDescription`s and Mac `applyStarterModelFilters` sort step. **Caption:** extra-filter parenthetical (`≤14B, tools+vision`) + appended `Sorted by newest.` sentence; wording byte-identical between C# and Swift. **Tests:** 51 new C# pins + 11 new Swift pins; CI green on second run after a `using System.ComponentModel;` fix for `SortDescription`/`ListSortDirection`. Previous: PR #257 M14 merged `31f1bbc` — Mac runner Pull-embedding-model button parity; PR #255 M11 merged `cf5713e` — accent-cyan row-count caption.)

Last released: **v1.3.22** (2026-05-10; F2a — full-page picker, search, Most popular cap, both OSes). **v1.3.23 dispatch in flight** (run 25645859600, cross-OS) — bundles M11/C1/M12/M13/C2/M14/C3+C4+C5.

> v1.2.7 tag exists on `af77abc` but has no GH release artifact; v1.2.8 supersedes it.

**Currently between tasks.** PR #259 (`9f81bd5`) shipped C3+C4+C5
as one cross-OS bundle, clearing the entire P1 F2a model-picker
cluster. **Post-v1.3.22 Mac field test (2026-05-10) surfaced four
follow-ons filed for next session** — see `project_backlog.md`
P1.5 cluster: **C24** (P0 regression — Mac refresh-catalog payload
missing `parametersBillion` + `lastUpdated`, makes Max-size and
Sort: Newest no-ops on Mac; one-line host fix), **C25** (visual
differentiation for capability pass-through entries), **C26**
(Most-popular limit dropdown 10/15/25/50 replacing the static 15),
**C27** (Hugging Face as a model source — full integration,
multi-stage, Opus planning at kickoff). User wants C24-C27 picked
up next session ahead of **P2 C6** (PrepApp detect-already-
configured-drive). Back-burnered: **M2** (signing + notarization,
awaiting Apple Developer cert renewal), **M10** (Mac PrepApp
finalize observability, cold-load wait acceptable post-MAC39).
Ancestor-walk consolidation into `prep-core/BundleContentRoots`
remains overdue.

## Recently shipped

- **PR #259 — C3+C4+C5 model picker filter cluster — merged `9f81bd5` (2026-05-10).** No new release tag yet — v1.3.23 dispatch in flight (run 25645859600). One PR bundles three F2a follow-ons reported on the v1.3.22 mac field test: parameter-cap dropdown (≤7B / ≤14B / ≤30B / ≤70B), capability AND chips (tools / vision / thinking / audio), sort dropdown (Popular / Newest / A–Z). **Scraper extension** (`prep-core/Services/LiveModelCatalogService.cs`): new `UpdatedRegex` for `x-test-updated` + `ParseRelativeDate` (months/years approximated for ordinal sort); new `ParseParamsBillions` handling MoE notation ("128x17b" → 128, largest-billion-wins). **Projection:** new `Capabilities: IReadOnlyList<string>`, `ParametersBillion: double?`, `LastUpdated: DateTimeOffset?` on `StarterModelEntry` + shared `StarterCatalogEntry` + `ModelGridRow` + Swift `StarterModelEntry`/`StarterModelDisplayEntry`; `mac-prep-host/HostLifetime.cs` forwards both new fields in `discover-catalog` and `refresh-catalog`. `lastUpdated` crosses the wire as ISO 8601 string (System.Text.Json default for `DateTimeOffset?`); Swift consumes as `String?` because ISO 8601 sorts lexically the same as the underlying instants — no `JSONDecoder` strategy fiddling. **Filter logic:** new `IsModelRowVisible` cap + AND-capability checks pass through null/empty data so configured/on-disk/bundled-pre-Refresh rows stay visible (same posture as F2a Most-popular for missing pull counts). New `ModelSortMode` enum (Popular / Newest / Alphabetical) — WPF translates to `SortDescription` on the `ListCollectionView`, Mac sorts the visible list directly via extended `applyStarterModelFilters`. **Caption:** `StarterRowCountCaption.Format` and `formatStarterRowCountCaption` gain extra-filter parenthetical (`≤14B, tools+vision`) and an appended `Sorted by newest.` sentence; wording byte-identical between C# and Swift. **UI:** second filter row below existing search/Most-popular row on both platforms; the WPF row uses `ComboBox`+`ToggleButton`+`ComboBox`, the Mac row uses SwiftUI `Picker` (menu) + Button-with-checkmark chips (macOS 11.0 baseline rules out `.toggleStyle(.button)`). **Tests:** 51 new C# pins (`ParseParamsBillions` + `ParseRelativeDate` Theories, scraper integration, caption branches, VM predicate + invalidation) + 11 new Swift pins. **CI:** green on second push after a `using System.ComponentModel;` import fix on the WPF code-behind (`SortDescription` / `ListSortDirection` aren't in WPF's implicit usings). Decision pinned in `project_decisions.md`.

- **PR #257 — M14 Mac runner "Pull embedding model" button — merged `31f1bbc` (2026-05-10).** No new release tag (Mac UX parity fix folds into next ship). Closes the C2 (PR #247) parity follow-up filed at MAC35-deferral pickup time. **MAC35 reframe:** the M14 filing in `mac_project_backlog.md:3490` listed Options A/B/C (parallel temp daemon / pause-and-restart prod daemon / user-coordinated idle) as if daemon-restart vs. parallel-daemon was the core tradeoff. **None of them applied.** Three findings from reading the code: (1) `ModelManagementService.PullEmbeddingModelAsync` is just `POST /api/pull` against the running Ollama daemon (`runner-core/Services/ModelManagementService.cs:94-109`); Ollama handles concurrent pull + chat requests; no restart, no parallel daemon — WPF has done it this way the entire project lifetime. (2) `IModelManagementService` is already DI-wired in the Mac sidecar (`mac-runner-host/HostLifetime.cs:154-156`) — the backing service was already there on Mac. (3) The Mac UI talks to the sidecar over HTTP (the same `RunnerLocalApiService` it uses for chat/library), not the stdin IPC, so the missing piece was a single HTTP route + a UI button. **New HTTP route:** `POST /api/models/embedding/pull` on `RunnerLocalApiService`, Bearer-auth gated by the existing middleware, returns 200 `{success, model}` on success and 503 + ProblemDetails on failure (missing service, missing Ollama host, missing config name, or pull failure). Companion clients pick up the route for free. **Mac UI:** new `pullEmbeddingModel()` method in `RunnerViewModel` (POSTs the route via `URLSession` with Bearer auth, surfaces success/failure via the existing `libraryStatus` banner — no new red/orange banner needed since this is an operational status, not a chat error); new "Pull embedding model" button in `DocumentsSection`'s action row, gated on `libraryBusy` only (the embedder is a global Ollama resource, not per-library, so it intentionally doesn't gate on `activeLibraryId` — matches WPF). **WPF Runner unchanged** — its in-process `_modelService.PullEmbeddingModelAsync(host, ...)` call keeps working; refactoring it to use the new HTTP route would add blast radius for zero benefit since both paths converge on the same `ModelManagementService` method. **Tests (3 new pins):** new `FakeModelManagementService` stub in `RunnerLocalApiServiceTests`, plus pins for (a) 200 with model name on success, (b) 503 + detail when the service returns false, (c) 401 when Bearer is missing — auth gate verified via the existing middleware. `RunnerLocalApiFixture.StartAsync` gained `modelService` + `embeddingModelName` parameters (defaulted; existing tests stay unchanged). **CI green first run** on all three jobs (mac-prep, mac-runner, windows). Decision pinned in `project_decisions.md`.

- **PR #255 — M11 row-count caption announces Most-popular toggle's effect — merged `cf5713e` (2026-05-10).** No new release tag (cross-OS UX fix folds into next ship). Closes the v1.3.22 mac field-test "Most popular doesn't do anything" report. **Root cause was perception, not logic.** Phase-1 instrumentation (orange `[M11]` log strip + per-toggle `appendLog`, plus a `staging` bypass to reach the picker without staged Mac artifacts) confirmed every layer worked: host forwards 398/399 pull counts as Int64 NSNumbers (`objCType=q`), Swift decoder preserves all 399 entries, toggle ON yields `visible=15` sorted desc by pulls. ollama.com's natural order is already popularity-desc, so toggling narrows 399 → 15 of the same first-screenful — user perceives no change. **Fix is pure UX:** new shared pure helper `FreeAiSsd.Shared.Models.StarterRowCountCaption.Format` lives in `shared/Models/`, with Swift mirror `formatStarterRowCountCaption` in `mac-prep-app/Sources/StarterCatalogTypes.swift` carrying byte-identical wording. WPF wires `OnPropertyChanged` once via constructor subscription to `ModelRowsViewInvalidated` + `ModelRows.CollectionChanged` (no per-callsite duplication). **Cross-OS bundle:** mac-prep-app + prep-app + shared + tests; 12 new pins. CI green first run on all three jobs.

- **PR #253 — C1 chat-stream heartbeat to keep large-model cold-loads alive — merged `6cfae14` (2026-05-10).** No new release tag. Closes the qwen3:14b chat-stall half of the v1.3.22 mac field-test report. **Root cause:** server flushes `start` immediately so Mac URLSession's 180s `timeoutIntervalForRequest` was bound by Ollama's first-token-after-cold-load latency. **Fix:** server-side heartbeat — new `IChatService.FirstTokenPending(int seconds)` event, fires every 20s while awaiting Ollama's first streamed token; `/chat/stream` forwards each tick as `{type:"loading", elapsedSeconds:N}` NDJSON; Mac paints `Loading <model>… NNs` in the title strip; Windows subscribes to the event in-process. Per-request `SemaphoreSlim` write-gate added to `/chat/stream` for HttpResponse stream serialization. Boundary logs (`chat stream begin`, `chat first-token in NNNNms`, `chat stream complete in NNNNms`) added via `SsdLogger.Info`. 2 new pins in `RunnerLocalApiServiceTests`. Cross-OS bundle.

- **PR #251 — M12 Mac runner chat UI parity to X13 — merged `a52572c` (2026-05-10).** No new release tag. New `chatError` red banner in `ContentView` covers the three X13 failure paths (mid-stream `error` frame, transport error, non-2xx HTTP body); new pure helper `RunnerChatErrorMessage.decode` extracted from a dead `apiErrorMessage` private method; `ChatStreamDelegate` now `.allow`s non-2xx responses and buffers the body for structured decoding. 7 new pins in standalone `RunnerChatErrorMessageTests` binary.

- **PR #249 — M13 Mac "Expose API on LAN" toggle no longer reverts itself — merged `e6b958e` (2026-05-10).** Root cause was the async `.stopped` callback overwriting user intent on every restart; fix removes the auto-clear from `.stopped` and lets `.crashed` own involuntary state changes.

- **PR #247 — C2 PrepApp auto-pulls embedding model + hardens ingest failure surface — merged `1df4431` (2026-05-10).** Stage 1: `PrepViewModel.EnsureEmbeddingModelInstalledAsync` idempotent pull during Download/Finalize. Stage 2: `EmbeddingClient.EmbedAsync` reads the 404 body and surfaces it instead of bare `EnsureSuccessStatusCode`; `DocumentIngestor` captures the first failure cause via `Interlocked.CompareExchange` and folds it into the threshold abort message. 8 new pins.

> Older entries (PR #173–#243 MAC1–MAC40 + F2a v1.3.22 — all merged 2026-05-05 through 2026-05-10) pruned to keep this section bounded. Full descriptions live in `git log` and the per-item entries in `agent_docs/mac_project_backlog.md` + `agent_docs/project_decisions.md`.

## Next up

**Authoritative priority lives in `project_backlog.md`** under the 2026-05-10 P0–P5 ranking. Highlights:

1. **P0 field-test bug surface — cleared.** M11, C1, M12, M13, C2, M14 all merged this session-cluster.
2. **P1 F2a model-picker cluster — cleared** (C3+C4+C5 bundled in PR #259 `9f81bd5`).
3. **P1.5 picker filter cluster follow-ons (new — 2026-05-10):** `C24` (Mac refresh-catalog payload regression; P0 priority within this cluster), `C25` (capability pass-through visual marker), `C26` (Most-popular limit dropdown), `C27` (Hugging Face source integration; Opus planning).
4. **P2 substantive UX:** `C6` (PrepApp detect-configured-drive flow).
5. **P3 critical pre-existing:** `C7` (was X9, encrypted config persistence), `C8` (was X10, document replacement consistency), `W1` (was X11, companion keyboard PTT), `C9` (was X12, download verify-before-move), `C10` (was F4 Stage 2, post-setup launch flow), `W2` (was F4 Stages 3-4, companion installer), `C11` (was B2, LAN discovery), `C12` (was R1 Stage 2, runner-cli slash-commands), `M7` (was MAC20, cross-platform release ZIP layout rework).
6. **P4 RAG audit batch** (slot when v1.3.x feature work resumes): `C13` (X18), `C14` (X15), `C15` (X19), `C16` (X20), `C17` (X22), `C18` (X23), `C19` (X17).
7. **Back-burnered per user:** `M2` (was MAC11, signing + notarization, awaiting Apple Developer cert renewal), `M10` (was MAC37, finalize observability, cold-load wait acceptable post-MAC39).
8. **Cleanup follow-up (now overdue):** fold `MacArtifactAvailability.EnumerateContentRoots`, `ArtifactStagingService.EnumerateBundledContentRoots`, and `PrereqService.EnumerateBundleRoots` into one shared `prep-core/BundleContentRoots` helper. All three are byte-identical ancestor-walk enumerators.

**RAG audit backlog:** X17-X23 cover audit findings; X10/X13/X15 scope expansions recorded. Plan: `C:\Users\Kninetimmy\.claude\plans\okay-i-want-to-glowing-galaxy.md`. v1.3.x sequence: X18 -> X15 (expanded) -> X19 -> X20 -> X22 -> X23. X17 reduced to Stage 1 textless-page diagnostic (full OCR deferred — workload is text-layer PDFs).

**Dormant (could not reproduce):** X1-Redux. Diag branch `diag/x1-redux-send-hang` stays on remote, unmerged.

See `project_backlog.md` for full general backlog details. See
`agent_docs/mac_project_backlog.md` for the macOS support track.

## Last session

2026-05-10 (latest, C3+C4+C5 session) (PR #259 merged `9f81bd5`). User picked the entire P1 F2a cluster from the post-v1.3.22 backlog ("yeah lets pull the plans for those" → "recon and bundle all three" → "looks good generate a prompt to follow then execute" → "merge it"). **Recon-grounded plan:** read `LiveModelCatalogService.ParseOllamaLibraryHtml` and confirmed against the `2026-05-07-snapshot.html` fixture that capabilities (`x-test-capability`) and size tokens (`x-test-size`) were already scraped — only `x-test-updated` needed adding. Selectors validated by grep (`audio/embedding/thinking/tools/vision` capabilities; `yesterday`/`N days/weeks/months/years ago` updated phrasing). **MoE notation surfaced from the fixture:** `128x17b` defaults to `Medium` under the existing `ClassifySize` (numeric parse fails); the new `ParseParamsBillions` handles it as largest-billion-wins (128) so memory-budget filters exclude MoE — pinned by a 4x7B unit test where the larger second number wins. **Wire format decision:** rejected piping `Date` across the C#/Swift bridge (would need `JSONDecoder` `dateDecodingStrategy` = `.iso8601` and risks breaking the existing decode path). Used `String?` since ISO 8601 sorts lexically the same as the underlying instants. **One CI failure caught:** `SortDescription` / `ListSortDirection` live under `System.ComponentModel`, not WPF's implicit usings — the local macOS build doesn't compile WPF projects so this slipped through pre-push. Fixed in a one-line follow-up commit; CI green second run. **Lessons worth pinning:** (1) when extending a scraper, grep the captured fixture for *all* `x-test-*` selectors first — `x-test-updated` was already there, the only question was whether to scrape it. (2) MoE size tokens are an edge case the existing `ClassifySize` silently fails open on; new numeric parsers must handle the format explicitly. (3) WPF code-behind has a different implicit-using set than the rest of the .NET projects — `System.ComponentModel` types compile fine in shared/test projects but require an explicit using in WPF.

2026-05-10 (M14 session) (PR #257 M14 merged `31f1bbc`). User picked M14 from the P0 list ("whats the next task" → "yes lets sketch a plan" → "start implementing"). **Plan grounded in code, not the backlog filing.** Read `runner-core/Services/ModelManagementService.cs:94-109` (PullEmbeddingModelAsync is just `POST /api/pull` against running Ollama; no daemon-restart needed), `mac-runner-host/HostLifetime.cs:154-156` (`IModelManagementService` already DI-wired in the sidecar — backing service was always there), and `mac-runner/Sources/main.swift` chat/library callsites (Mac UI talks to sidecar over HTTP, not stdin IPC). **This disproved the MAC35-deferral framing in `mac_project_backlog.md:3490`** — the filing presented daemon-restart vs. parallel-daemon vs. user-coordinated-idle as the core tradeoff; none applied because Ollama handles concurrent pull + chat natively (WPF has done it the entire project lifetime) and the Mac UI just needed an HTTP route to reach the model service. **Implementation:** new `POST /api/models/embedding/pull` route under existing Bearer-auth middleware, returning 200 `{success, model}` on success and 503 + ProblemDetails on failure (4 failure paths covered: missing service, missing Ollama host, missing config name, pull-returned-false). Mac VM gets `pullEmbeddingModel()` POSTing the route + a button in `DocumentsSection`'s action row gated only on `libraryBusy` (not on active library — embedder is global). WPF runner unchanged — its in-process call keeps working; refactoring to the HTTP route would add blast radius for no benefit. **Tests:** new `FakeModelManagementService` stub in `RunnerLocalApiServiceTests`, fixture gained `modelService` + `embeddingModelName` params with defaults so existing tests stayed unchanged, 3 pins (200/503/401). **CI green first run** on all three jobs. **Lessons worth pinning:** (1) read the code before trusting an old backlog filing — MAC35 was authored before the Mac sidecar's HTTP architecture was fully understood, so its "deferred over daemon-restart concerns" framing aged poorly. (2) When a feature lives in a separate process from its backing service, the natural surface is the existing HTTP API, not a new IPC command — Mac runner UI/sidecar already had the HTTP transport in place. (3) Companion + Mac UI both consume the same Runner API surface, so a new route for Mac parity is automatically a Companion capability without extra work.

> Older entries (M11, C1, M12, M13, C2, F2a — all 2026-05-10 same-session merges) pruned to keep this section to the 2 most recent dated entries per the wrap-up rule. Full descriptions live in `git log` and the PR bodies.

## Open questions

- **C3/C4/C5 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — F2a follow-ons reported here originally):** PrepApp picker → Refresh from Ollama → set Max size to ≤14B; rows above 14B disappear, configured + on-disk + bundled rows stay visible (null-params pass-through). Caption reads `Showing N of 399 matching filter (≤14B).` Click `tools` chip → capability AND filter narrows to tool-capable models only; combine with `vision` chip → only multi-cap entries survive (entries with empty caps still pass through so configured rows are unaffected). Pick Sort: Newest → entries reorder by scraped `x-test-updated` desc; nil-date entries (bundled, configured) sort last; caption gains ` Sorted by newest.` Pick Sort: A–Z → tag-ascending. **Windows parity:** Models tab → Refresh → same three controls in the second filter row, same caption strings, same pass-through posture for configured/on-disk rows. **Empty-data edge:** pre-Refresh on a freshly-prepped drive → capability chips and Max size cap are no-ops on bundled-only entries (no caps/params data); Sort: Newest sorts only by what little data exists, nils last.

- **M14 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Mac Runner → unlock SSD → wait for `networkApiBaseUrl` to populate → Documents section → click "Pull embedding model". `libraryStatus` reads `Pulling embedding model…` then `Embedding model ready: nomic-embed-text:latest`. Force-quit Ollama via the runner's Stop control and click again → status surfaces the 503 reason (`Ollama host is not running. Start Ollama before pulling the embedding model.`). Companion-app parity: a Companion client with the same Bearer key can `POST /api/models/embedding/pull` and receive the same 200/503 shape. WPF runner unchanged → confirm by clicking the existing "Pull embedding model" button in the Index tools expander; behavior identical to pre-M14.

- **M11 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** PrepApp picker → Refresh from Ollama → click Most-popular ON. New accent-cyan line appears between the catalog status text and the picker reading `Showing top 15 of 399 by pulls.` (or whatever the live total is). Click OFF → line disappears. Type `llama` in search → reads `Showing N of 399 matching search.` Combine search + Most-popular → reads `Showing top N of 399 by pulls (filtered by search).` **Windows parity:** Models tab → Refresh → toggle Most popular. Same caption strings appear above the merged Models grid; configured + on-disk rows still surface (the top-15 cap only applies to Recommended-source rows, but the caption counts the entire visible ModelRows set). **Empty-catalog edge:** pre-Refresh on a freshly-prepped drive → caption stays empty under both toggle states (the existing "Refresh first" empty-state branch handles UX guidance).

- **C1 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** load `qwen3:14b` cold (force-quit Ollama first or pick a freshly-prepped SSD), Send → status reads `Loading qwen3:14b… 20s` then `…40s` then `…60s` etc. through the cold-load duration; flips to `Generating…` once the first token arrives; response area fills normally; final status `Answered` or `Answered with sources`. **SSD log evidence:** `chat stream begin (model=qwen3:14b, host=…)`, then a long gap, then `chat first-token in NNNNms (model=qwen3:14b)` and `chat stream complete in NNNNms`. **Warm-load contract:** second Send within 5 min of first → no `loading` frames in NDJSON, status goes `Sending... → Generating… → Answered`. **Windows parity:** same model + cold-load on Windows runner → `StreamingIndicator` reads `● Loading model… NNs` until tokens flow; PTT chat (which uses streaming under the hood) does NOT paint the loading text in the chat tab (visibility gate suppresses). **Stress pin:** if heartbeats keep flowing past 180s and no token ever arrives, that's the residual phase-1 case — Ollama-side load issue (memory pressure / corrupted blob); user can hit Stop and report. Heartbeat timer + write-gate prevent the silent-timeout class of bugs from ever recurring; this pin verifies the behavior end-to-end.

- **M12 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Send with empty model selected → red banner reads `"Model is required."` (validation 400 body). Force-quit `mac-runner-host` mid-request → red banner reads the URLSession transport reason (e.g., `"The network connection was lost."`); status strip also reads `Chat failed: ...`. Trigger a `RagRetrievalFailed` (e.g., temporarily delete the embedder) → orange `ragWarning` banner shows the RAG reason, response area shows the model's answer-without-context, no red banner. Mid-stream backend failure (e.g., kill ollama mid-stream) → red banner shows the `error` NDJSON frame message; file log gains `Chat backend error: <msg>`. Library ops with sidecar down → `libraryStatus` shows the bare reason (e.g., `"Service unavailable."`) without the now-removed `"Chat failed: "` prefix.

- **M13 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Click "Expose API on LAN" toggle ON → toggle stays ON; runner log shows `API host bound to <configured-bind>` (not `127.0.0.1`); `networkApiStatus` reads `API: <baseUrl>` with the LAN address. Click toggle OFF → toggle stays OFF; log shows `API host bound to 127.0.0.1`. Toggle ON, then force-quit the sidecar process from Activity Monitor → toggle reverts to OFF with `networkApiStatus` showing the crash message (proving `.crashed` path still clears intent). Lock + Unlock with toggle persisted ON in config → toggle hydrates ON and stays ON after auto-restart.

- **PR #247 C2 field-test pin (new — awaiting first run on a fresh-prepped Mac SSD):** drop a sub-1MB PDF into a new library → ingest succeeds end-to-end without manual embed-model setup. Re-finalize a fully-prepped drive → log shows `Embedding model nomic-embed-text:latest already on disk; skipping pull` and no temp Ollama server is spun up. Failure-message pin: temporarily delete the embedder from the SSD, re-ingest → error names `model 'nomic-embed-text' not found` rather than just the threshold ratio.

- **v1.3.22 mac + windows field-test pin (F2a) — released 2026-05-10 same session; awaiting first field run.** **Mac side now also covered by the M11 pin above** — the field-test confirms both the underlying filter behavior AND the new caption that announces the cap. **Mac picker smoke (priority):** PrepApp picker fills the page; resizing the window grows the list; search filters live as user types; clicking "Most popular" before Refresh shows the empty state with hint to Refresh first; after Refresh, Most popular shows ~15 entries sorted by pull count desc + the M11 caption announces the cap; clearing search/popular returns the full list and the caption hides. **Windows picker smoke:** Models tab gets the new search + Most popular row + M11 caption; same filter behavior; configured + on-disk rows stay visible even with Most popular on.
- **MAC37 driver pin (still applicable but back-burnered):** measure how long Mac PrepApp finalize takes on a fresh pull; observe whether it's actually doing work or waiting on a timeout. Not blocking — user accepted the 20s post-cache wait.
- **MAC35a/MAC34b/MAC35 deferred field-test smokes (still applicable on v1.3.22):** AppleDouble sidecar regression — re-running readiness on a v1.3.15-prepped SSD passes all-green; port 11434 reclaim — Ollama.app already running → Runner.app log shows "Found N existing process(es) on port 11434 (PIDs ...)"; pull cancel/resume scenarios — partials persist, no torn blobs after re-run; disk-full precheck fires when staging volume has < 2× model size free.
- **F2 manual smoke (partially complete on v1.3.5 mac field test):** Mac side confirmed at v1.3.5 + F2a at v1.3.22. **Windows side still deferred** to a Windows machine: online → click Refresh → grid repopulates; offline → caption explains failure, bundled list stays in place.
- **MAC20 still pending:** bundle root scatters Windows DLLs alongside `payload/` until MAC20 reshapes to `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE`.
- Before a public signed Mac beta, verify whether the nested `payload/mac/Runner.app.zip` (or whatever path MAC20 lands on) preserves the stapled notarization ticket after users download and extract the cross-platform ZIP. If not, ship a standalone notarized app ZIP or DMG.
- **MAC6 manual smoke (deferred until a Mac on the same LAN as a Windows Companion):** Windows Companion discovers and connects to a Mac-hosted Runner with Bearer auth + /api/health + /api/chat; Network Mode toggle with real Ollama serving a real model end-to-end.
- **MAC8 manual smoke (deferred to a real Mac):** Network Mode on → Documents UI populates → Create library → Add Files (TXT) → ingest completes → Send chat → returns sources from the uploaded file.
- **MAC10b manual visual smoke (deferred to a real Mac + a Windows machine):** Runner.app shows the Free-AI-SSD icon in Finder, the Dock, and command-tab on a clean Mac; WPF Runner / PrepApp / Companion show the icon in title bars and the Windows taskbar.
- **MAC17 manual smoke (deferred to a real Mac + an external SSD):** plug an external SSD → launch `PrepApp.app` → drive shows in selection list → choose Mac-only or cross-platform target → format → SSD layout → Mac Runner staged → Mac Ollama staged → starter model pulls → encryption setup with passphrase → readiness all green → finalize. Plus the cross-platform roundtrip.

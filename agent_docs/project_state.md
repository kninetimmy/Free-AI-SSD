# Project State

Last updated: 2026-05-11 (latest) (PR #266 C27 Stage 1 merged `58f79a1` — Hugging Face as a model source, anonymous read only. New `HuggingFaceCatalogService` in prep-core against HF's GGUF Search API; new `ModelSource` enum (Ollama / HuggingFace) plumbed through `StarterCatalogEntry` → `StarterModelEntry` → `ModelGridRow`. WPF: Source `ComboBox` between title + Refresh, source-aware refresh dispatch, 350ms `DispatcherTimer` search debounce, cancel-and-restart on switch. Mac: SwiftUI `Picker` + `handleSourceSwitch` + debounced `scheduleHuggingFaceSearch` via Task.sleep; new `discover-hf-catalog` + `search-hf` IPC arms on mac-prep-host. C24 lesson cashed in via shared `BuildCatalogEntries` projection feeding all four catalog-emitting arms. 24 new HF-service pins + 4 VM pins + 9 Swift pins. CI green first run. Previous: PR #264 C25+C26 merged `3f299fe` — picker filter cluster polish; PR #262 C24 merged `34a66b8` — Mac refresh-catalog payload regression.)

Last released: **v1.3.22** (2026-05-10; F2a — full-page picker, search, Most popular cap, both OSes). **v1.3.23 dispatch in flight** (run 25645859600, cross-OS) — bundles M11/C1/M12/M13/C2/M14/C3+C4+C5. **C24/C25/C26/C27 Stage 1 (PRs #262/#264/#266) roll into the next dispatch** (v1.3.24).

> v1.2.7 tag exists on `af77abc` but has no GH release artifact; v1.2.8 supersedes it.

**Currently between tasks.** PR #266 (`58f79a1`) closed **C27
Stage 1** — Hugging Face catalog adapter + Source dropdown
landed on both OSes; HF rows are browse-only this stage
(`DownloadAsync` refuses any batch with HF rows so a
half-fulfilled download UX can't ship). Remaining C27 work:
**Stage 2** (HF pull integration via `hf.co/...` tags +
`siblings[].size` disk-budget warnings), **Stage 3** (HF token
auth via encrypted-config storage; private + gated repos),
**Stage 4** (per-quant row expansion + capability inference).
User to choose whether to chain Stage 2 next or pivot to
**P2 C6** (PrepApp detect-already-configured-drive). Back-burnered:
**M2** (signing + notarization, awaiting Apple Developer cert
renewal), **M10** (Mac PrepApp finalize observability, cold-load
wait acceptable post-MAC39). Ancestor-walk consolidation into
`prep-core/BundleContentRoots` remains overdue.

## Recently shipped

- **PR #266 — C27 Stage 1 Hugging Face as a model source — merged `58f79a1` (2026-05-11).** No new release tag (rolls into next dispatch, v1.3.24). Stage 1 of 4 — anonymous read only; pulls + token auth + per-quant rows defer to Stages 2/3/4. **Service:** new `prep-core/Services/HuggingFaceCatalogService.cs` against `https://huggingface.co/api/models?filter=gguf&sort=downloads&limit=N` (mirrors `LiveModelCatalogService` shape — HTTPS-only allowlist, owned HttpClient, typed `LiveCatalogFetchException` reused, in-memory cache, User-Agent header, no Authorization). Defensive `tags` re-check after the server-side filter; best-effort `TryExtractParamsFromRepoId` (MoE-aware via largest-billion-wins, same posture as `LiveModelCatalogService.ParseParamsBillions`). **Shared:** new `ModelSource` enum + `Source` field on `StarterCatalogEntry` (record evolution at end; default `Ollama` so JSON-deserialized bundled-catalog and existing scrape projections compile unchanged) + `SourceKind` on `ModelGridRow` + `Source` init on `StarterModelEntry`. **VM:** new `PrepViewModel.ActiveSource` + `ActiveSourceChanged` event; `DownloadAsync` refuses any batch including an HF row with a Stage-2 explanation. **WPF UI:** Source `ComboBox` between title + Refresh; new handler `SourceCombo_SelectionChanged` flips VM; `OnActiveSourceChanged` cancels in-flight refresh and dispatches the appropriate fetch; 350ms `DispatcherTimer` debounce on search box when source is HF; refactored `RefreshCatalogButton_Click` dispatches by source. **Mac UI:** SwiftUI `Picker` next to Refresh; `handleSourceSwitch` clears selections + catalog and routes; `scheduleHuggingFaceSearch` uses `Task.sleep`-based debounce; new `discover-hf-catalog` + `search-hf` IPC arms on `mac-prep-host/HostLifetime.cs`; new `ModelSourceKind` enum + `parse(_:)` with `.ollama` fallback in `StarterCatalogTypes.swift`. **C24 lesson cashed in:** new shared `HostLifetime.BuildCatalogEntries` helper backs all four catalog-emitting arms (discover-catalog / refresh-catalog / discover-hf-catalog / search-hf) — adding a wire field becomes a one-site change. **Tests:** 24 new C# pins on HuggingFaceCatalogService (allowlist, HTTPS, cancellation, timeout, 5xx, 429 status code, malformed JSON, query URL + encoding, UA / no-Auth headers, cache reuse, GGUF re-check, projection, MoE param extraction) + 4 VM pins for `ActiveSource` defaults / events / source-tagging / HF download refusal. 9 new Swift pins covering `ModelSourceKind.parse` (incl. nil + unknown fallback), wire round-trip via `decodeStarterEntries` + `StarterModelDisplayEntry.from`, capability-filter pass-through under HF (C25 posture), Most-popular + Newest sort participation, nil-`parametersBillion` cap-filter pass-through. Captured response fixture at `tests/Fixtures/HuggingFace/2026-05-11-popular-gguf.json`. CI green first run on all three jobs. Decision pinned in `project_decisions.md` (catalog projection shape — single discriminator field).

- **PR #264 — C25+C26 picker filter cluster polish — merged `3f299fe` (2026-05-11).** No new release tag (rolls into next dispatch, v1.3.24). One PR bundles two P1.5 polish items filed post-v1.3.22 Mac field test. **C25 (capability pass-through marker, Option A):** new `HasActiveCapabilityFilter` derived property on `PrepViewModel` (returns `_requiredCapabilities.Count > 0`; `PropertyChanged` raised inside `SetCapabilityFilter`). WPF: `DataGrid.RowStyle` `MultiDataTrigger` with two conditions (`Capabilities.Count == 0` AND `DataContext.HasActiveCapabilityFilter == True`, the latter resolved via `RelativeSource AncestorType=DataGrid`) sets `Opacity=0.55` + a tooltip explaining the pass-through. Mac: SwiftUI `.opacity()` + `.help()` on the row body keyed off `entry.capabilities.isEmpty && !vm.requiredCapabilities.isEmpty`. Picker stays quiet in the default view — marker only appears when the user has narrowed by chip. **C26 (Most-popular limit dropdown):** `const int MostPopularLimit = 15` becomes instance property `MostPopularLimit` (default 15 preserved via new `DefaultMostPopularLimit` const) + static `MostPopularLimitOptions = [10, 15, 25, 50]`; setter recomputes `_topPopularStarterTags` and raises `ModelRowsViewInvalidated`. WPF: new `ComboBox` next to the Most-popular `ToggleButton`, `SelectionChanged` handler reads `Tag` → VM. Mac: `static let mostPopularCount` → `@Published var mostPopularLimit`; SwiftUI `Picker(.menu)` next to the Most-popular `Button`; `visibleStarterModels` switches from `Self.mostPopularCount` to the instance property. Generalized "top 15" tooltips to "top-N". **Tests:** 4 new C# pins (`HasActiveCapabilityFilter` default + toggle; `MostPopularLimit` default + change-recomputes-and-invalidates + no-op-set-no-fire; `MostPopularLimitOptions` surface pin); 5 new Swift pins (C26 caps at 10, expands to 25; C25 pass-through flag true/false/false). One existing C# test rewired from `PrepViewModel.MostPopularLimit` const to `PrepViewModel.DefaultMostPopularLimit`. CI green first run on all three jobs. Decision pinned in `project_decisions.md`.

- **PR #262 — C24 Mac refresh-catalog payload regression — merged `34a66b8` (2026-05-11).** No new release tag (rolls into next dispatch, v1.3.24). P0 regression introduced by PR #259. `mac-prep-host/HostLifetime.cs` refresh-catalog projection at line 522-530 forwarded `tag/params/sizeTier/description/useCases/pullCount` but dropped `parametersBillion` and `lastUpdated` — both were added to the sibling discover-catalog arm at line 473-483 in PR #259 but never propagated to refresh. Mac PrepApp's Max-size cap and Sort: Newest dropdowns were no-ops after Refresh from Ollama; caption reported correct filtered counts (e.g. "Showing 275 of 399 matching filter (≤7B, thinking+tools)") but the list never reordered or shrank. WPF unaffected — its refresh path goes through `LiveModelCatalogService.FetchAsync` in-process and never crosses the JSON wire. **Fix:** two added lines mirror the discover-catalog projection. **Tests (2 new Swift pins):** synthetic refresh-catalog payload as `[String: Any]` → `decodeStarterEntries` → `applyStarterModelFilters` with cap=7 and `sortMode: .newest`; both pins assert the *round-trip* value (e.g. `big?.parametersBillion == 30.0`) *and* the downstream filter/sort behavior, so they fail on the pre-fix wire shape (decoded nil → cap pass-through keeps 30B; nil `lastUpdated` → newest sort is identity). CI green first run on all three jobs. Decision pinned in `project_decisions.md`.

> Older entries (PR #173–#259 MAC1–MAC40 + F2a v1.3.22 + M11/C1/M12/M13/C2/M14 + C3+C4+C5 — all merged 2026-05-05 through 2026-05-10) pruned to keep this section bounded. Full descriptions live in `git log` and the per-item entries in `agent_docs/mac_project_backlog.md` + `agent_docs/project_decisions.md`.

## Next up

**Authoritative priority lives in `project_backlog.md`** under the 2026-05-10 P0–P5 ranking. Highlights:

1. **P0 field-test bug surface — cleared.** M11, C1, M12, M13, C2, M14 all merged this session-cluster.
2. **P1 F2a model-picker cluster — cleared** (C3+C4+C5 bundled in PR #259 `9f81bd5`).
3. **P1.5 picker filter cluster follow-ons — partial.** C24/C25/C26 + **C27 Stage 1** all closed (PRs #262, #264, #266). Remaining C27 stages: **Stage 2** (HF pull via `hf.co/...` + disk-budget), **Stage 3** (token auth + private/gated repos), **Stage 4** (per-quant row expansion + capability inference).
4. **P2 substantive UX:** `C6` (PrepApp detect-configured-drive flow). User may pick this over C27 Stage 2 to break up multi-stage work.
5. **P3 critical pre-existing:** `C7` (was X9, encrypted config persistence), `C8` (was X10, document replacement consistency), `W1` (was X11, companion keyboard PTT), `C9` (was X12, download verify-before-move), `C10` (was F4 Stage 2, post-setup launch flow), `W2` (was F4 Stages 3-4, companion installer), `C11` (was B2, LAN discovery), `C12` (was R1 Stage 2, runner-cli slash-commands), `M7` (was MAC20, cross-platform release ZIP layout rework).
6. **P4 RAG audit batch** (slot when v1.3.x feature work resumes): `C13` (X18), `C14` (X15), `C15` (X19), `C16` (X20), `C17` (X22), `C18` (X23), `C19` (X17).
7. **Back-burnered per user:** `M2` (was MAC11, signing + notarization, awaiting Apple Developer cert renewal), `M10` (was MAC37, finalize observability, cold-load wait acceptable post-MAC39).
8. **Cleanup follow-up (now overdue):** fold `MacArtifactAvailability.EnumerateContentRoots`, `ArtifactStagingService.EnumerateBundledContentRoots`, and `PrereqService.EnumerateBundleRoots` into one shared `prep-core/BundleContentRoots` helper. All three are byte-identical ancestor-walk enumerators.

**RAG audit backlog:** X17-X23 cover audit findings; X10/X13/X15 scope expansions recorded. Plan: `C:\Users\Kninetimmy\.claude\plans\okay-i-want-to-glowing-galaxy.md`. v1.3.x sequence: X18 -> X15 (expanded) -> X19 -> X20 -> X22 -> X23. X17 reduced to Stage 1 textless-page diagnostic (full OCR deferred — workload is text-layer PDFs).

**Dormant (could not reproduce):** X1-Redux. Diag branch `diag/x1-redux-send-hang` stays on remote, unmerged.

See `project_backlog.md` for full general backlog details. See
`agent_docs/mac_project_backlog.md` for the macOS support track.

## Last session

2026-05-11 (latest, C27 Stage 1 session) (PR #266 merged `58f79a1`). User picked C27 from P1.5 follow-on cluster; explicit Opus planning pass produced a 10-section plan (decisions resolved (6, incl. 2 surfaced during planning), file-by-file change list, HF API surface, cross-OS IPC bridge, UI placement, source-switch behavior, test plan, security-invariants checklist, out-of-scope, risks/unknowns). User approved the plan and the 3 flagged risks (HF Download refusal posture over disable-button, source-aware search tooltip wording, cancel-and-restart on switch). Implementation mirrored `LiveModelCatalogService`'s shape; `BuildCatalogEntries` shared helper cashes in the C24 lesson by collapsing four projection sites into one. Pre-existing test failures (4 — Windows-only path/PS tests + a smoke test wanting a published mac-runner-host artifact) match exactly the set documented as unrelated in prior wrap-ups; CI green first run. 24+4 C# pins + 9 Swift pins. **Cross-OS audit (per parity rule):** both pickers got mirrored Source dropdowns + debounced searches + IPC arms — no parity gap. Decision pinned in `project_decisions.md`: multi-source catalogs use a single discriminator field on the shared entry, not parallel types.

2026-05-11 (C25+C26 session) (PR #264 merged `3f299fe`). User picked C25 + C26 from the P1.5 follow-on cluster, asked whether they could be bundled or had to be split. Bundled per file-overlap argument (both touch `shared/ViewModels/PrepViewModel.cs`, `prep-app/MainWindow.xaml(.cs)`, `mac-prep-app/Sources/main.swift` + `PrepViewModel.swift`) — same precedent as C3+C4+C5 (PR #259). User explicitly picked Option A for C25 (per-row opacity marker over caption-only or segmented-sections). **Implementation surprise (none worth pinning to lessons beyond the decisions entry):** the WPF `MultiDataTrigger` cleanly binds row property + ancestor VM property via two `<Condition>` lines without needing an `IMultiValueConverter`; the `DataContext.HasActiveCapabilityFilter, RelativeSource={RelativeSource AncestorType=DataGrid}` form was the right reach. **Pre-existing test failures (3 of 4) confirmed unrelated to my changes** via `git stash` + retest on clean main — `DriveFormatCommandTests.Build_UsesAbsolutePowerShellPath`, `ConfigStoreTests.SaveEncryptedConfig_RollsBackBlob_WhenStateRenameFails`, `PathGuardsTests.IsPathUnderRoot_WindowsBoundaryIsRespected` (Windows-only path/PS tests fail on macOS host). The 4th (`MacRunnerHostSmokeTests.PublishedHost_ServesHealthAndChat_WithStubChat`) wants a published mac-runner-host artifact. CI green first run on all three jobs. **Cross-OS audit (per the user's parity rule):** both pickers got mirrored changes; no gap to file. Decision pinned in `project_decisions.md`: VM exposes derived `HasActiveXFilter` so per-row visual cues only render when the filter is engaged — applies to future picker chip features (e.g., C27 HF).

> Older entries (C24, C3+C4+C5, M14, M11, C1, M12, M13, C2 — all 2026-05-10/11 merges) pruned to keep this section to the 2 most recent dated entries per the wrap-up rule. Full descriptions live in `git log` and the PR bodies.

## Open questions

- **v1.3.24 field-test pin (awaiting first run, both OSes; bundles C3/C4/C5/C24/C25/C26/C27 Stage 1):** **C3/C4/C5/C24 (Mac priority — F2a follow-ons reported here originally):** PrepApp picker → Refresh from Ollama → set Max size to ≤14B; rows above 14B disappear, configured + on-disk + bundled rows stay visible (null-params pass-through). Caption reads `Showing N of 399 matching filter (≤14B).` Click `tools` chip → capability AND filter narrows to tool-capable models only; combine with `vision` chip → only multi-cap entries survive (entries with empty caps still pass through so configured rows are unaffected). Pick Sort: Newest → entries reorder by scraped `x-test-updated` desc; nil-date entries (bundled, configured) sort last; caption gains ` Sorted by newest.` Pick Sort: A–Z → tag-ascending. **Windows parity:** Models tab → Refresh → same three controls in the second filter row, same caption strings, same pass-through posture for configured/on-disk rows. **Empty-data edge:** pre-Refresh on a freshly-prepped drive → capability chips and Max size cap are no-ops on bundled-only entries; Sort: Newest sorts only by what little data exists, nils last. **C25 marker:** with at least one chip engaged, rows with empty Capabilities render at opacity 0.55 with a tooltip; chips off → marker disappears. **C26 dropdown:** Most-popular limit dropdown switches between Top 10/15/25/50. **C27 Stage 1:** Source dropdown shows Ollama / Hugging Face; switching to HF clears the catalog and loads ~50 popular GGUF rows from `huggingface.co/api/models?filter=gguf`. Type "qwen" in search → debounced (350ms) `search-hf` fires; results re-populate. Capability chip with HF rows engaged → all HF rows survive (empty caps + C25 marker). Sort: Newest → HF rows reorder by `lastModified`. Check an HF row + click Download → refused with "Hugging Face pulls land in Stage 2" status text; no pull attempted. 429 response (rare; hard to provoke in field test) → "Hugging Face is rate-limiting requests" caption. Switching source back to Ollama restores the bundled list immediately.

- **M14 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Mac Runner → unlock SSD → wait for `networkApiBaseUrl` to populate → Documents section → click "Pull embedding model". `libraryStatus` reads `Pulling embedding model…` then `Embedding model ready: nomic-embed-text:latest`. Force-quit Ollama via the runner's Stop control and click again → status surfaces the 503 reason (`Ollama host is not running. Start Ollama before pulling the embedding model.`). Companion-app parity: a Companion client with the same Bearer key can `POST /api/models/embedding/pull` and receive the same 200/503 shape. WPF runner unchanged → confirm by clicking the existing "Pull embedding model" button in the Index tools expander; behavior identical to pre-M14.

- **M11 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** PrepApp picker → Refresh from Ollama → click Most-popular ON. New accent-cyan line appears between the catalog status text and the picker reading `Showing top 15 of 399 by pulls.` (or whatever the live total is). Click OFF → line disappears. Type `llama` in search → reads `Showing N of 399 matching search.` Combine search + Most-popular → reads `Showing top N of 399 by pulls (filtered by search).` **Windows parity:** Models tab → Refresh → toggle Most popular. Same caption strings appear above the merged Models grid; configured + on-disk rows still surface (the top-15 cap only applies to Recommended-source rows, but the caption counts the entire visible ModelRows set). **Empty-catalog edge:** pre-Refresh on a freshly-prepped drive → caption stays empty under both toggle states (the existing "Refresh first" empty-state branch handles UX guidance).

- **C1 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** load `qwen3:14b` cold (force-quit Ollama first or pick a freshly-prepped SSD), Send → status reads `Loading qwen3:14b… 20s` then `…40s` then `…60s` etc. through the cold-load duration; flips to `Generating…` once the first token arrives; response area fills normally; final status `Answered` or `Answered with sources`. **SSD log evidence:** `chat stream begin (model=qwen3:14b, host=…)`, then a long gap, then `chat first-token in NNNNms (model=qwen3:14b)` and `chat stream complete in NNNNms`. **Warm-load contract:** second Send within 5 min of first → no `loading` frames in NDJSON, status goes `Sending... → Generating… → Answered`. **Windows parity:** same model + cold-load on Windows runner → `StreamingIndicator` reads `● Loading model… NNs` until tokens flow; PTT chat (which uses streaming under the hood) does NOT paint the loading text in the chat tab (visibility gate suppresses). **Stress pin:** if heartbeats keep flowing past 180s and no token ever arrives, that's the residual phase-1 case — Ollama-side load issue (memory pressure / corrupted blob); user can hit Stop and report. Heartbeat timer + write-gate prevent the silent-timeout class of bugs from ever recurring; this pin verifies the behavior end-to-end.

- **M12 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Send with empty model selected → red banner reads `"Model is required."` (validation 400 body). Force-quit `mac-runner-host` mid-request → red banner reads the URLSession transport reason (e.g., `"The network connection was lost."`); status strip also reads `Chat failed: ...`. Trigger a `RagRetrievalFailed` (e.g., temporarily delete the embedder) → orange `ragWarning` banner shows the RAG reason, response area shows the model's answer-without-context, no red banner. Mid-stream backend failure (e.g., kill ollama mid-stream) → red banner shows the `error` NDJSON frame message; file log gains `Chat backend error: <msg>`. Library ops with sidecar down → `libraryStatus` shows the bare reason (e.g., `"Service unavailable."`) without the now-removed `"Chat failed: "` prefix.

- **M13 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Click "Expose API on LAN" toggle ON → toggle stays ON; runner log shows `API host bound to <configured-bind>` (not `127.0.0.1`); `networkApiStatus` reads `API: <baseUrl>` with the LAN address. Click toggle OFF → toggle stays OFF; log shows `API host bound to 127.0.0.1`. Toggle ON, then force-quit the sidecar process from Activity Monitor → toggle reverts to OFF with `networkApiStatus` showing the crash message (proving `.crashed` path still clears intent). Lock + Unlock with toggle persisted ON in config → toggle hydrates ON and stays ON after auto-restart.

- **PR #247 C2 field-test pin (new — awaiting first run on a fresh-prepped Mac SSD):** drop a sub-1MB PDF into a new library → ingest succeeds end-to-end without manual embed-model setup. Re-finalize a fully-prepped drive → log shows `Embedding model nomic-embed-text:latest already on disk; skipping pull` and no temp Ollama server is spun up. Failure-message pin: temporarily delete the embedder from the SSD, re-ingest → error names `model 'nomic-embed-text' not found` rather than just the threshold ratio.

- **v1.3.22 mac + windows field-test pin (F2a) — released 2026-05-10 same session; awaiting first field run.** **Mac side now also covered by the M11 pin above** — the field-test confirms both the underlying filter behavior AND the new caption that announces the cap. **Mac picker smoke (priority):** PrepApp picker fills the page; resizing the window grows the list; search filters live as user types; clicking "Most popular" before Refresh shows the empty state with hint to Refresh first; after Refresh, Most popular shows ~15 entries sorted by pull count desc + the M11 caption announces the cap; clearing search/popular returns the full list and the caption hides. **Windows picker smoke:** Models tab gets the new search + Most popular row + M11 caption; same filter behavior; configured + on-disk rows stay visible even with Most popular on.
- **MAC37 driver pin (still applicable but back-burnered):** measure how long Mac PrepApp finalize takes on a fresh pull; observe whether it's actually doing work or waiting on a timeout. Not blocking — user accepted the 20s post-cache wait.
- **MAC35a/MAC34b/MAC35 deferred field-test smokes (still applicable on v1.3.22):** AppleDouble sidecar regression — re-running readiness on a v1.3.15-prepped SSD passes all-green; port 11434 reclaim — Ollama.app already running → Runner.app log shows "Found N existing process(es) on port 11434 (PIDs ...)"; pull cancel/resume scenarios — partials persist, no torn blobs after re-run; disk-full precheck fires when staging volume has < 2× model size free.
- **F2 manual smoke (partially complete on v1.3.5 mac field test):** Mac side confirmed at v1.3.5 + F2a at v1.3.22. **Windows side still deferred** to a Windows machine: online → click Refresh → grid repopulates; offline → caption explains failure, bundled list stays in place.
- **MAC20 still pending:** bundle root scatters Windows DLLs alongside `payload/` until MAC20 reshapes to `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE`.
- Before a public signed Mac beta, verify whether the nested `payload/mac/Runner.app.zip` (or whatever path MAC20 lands on) preserves the stapled notarization ticket after users download and extract the cross-platform ZIP. If not, ship a standalone notarized app ZIP or DMG.
- **MAC6 manual smoke (deferred until a Mac on the same LAN as a Windows Companion):** Windows Companion discovers and connects to a Mac-hosted Runner with Bearer auth + /api/health + /api/chat; Network Mode toggle with real Ollama serving a real model end-to-end.
- **MAC8 manual smoke (deferred to a real Mac):** Network Mode on → Documents UI populates → Create library → Add Files (TXT) → ingest completes → Send chat → returns sources from the uploaded file.
- **MAC10b manual visual smoke (deferred to a real Mac + a Windows machine):** Runner.app shows the Free-AI-SSD icon in Finder, the Dock, and command-tab on a clean Mac; WPF Runner / PrepApp / Companion show the icon in title bars and the Windows taskbar.
- **MAC17 manual smoke (deferred to a real Mac + an external SSD):** plug an external SSD → launch `PrepApp.app` → drive shows in selection list → choose Mac-only or cross-platform target → format → SSD layout → Mac Runner staged → Mac Ollama staged → starter model pulls → encryption setup with passphrase → readiness all green → finalize. Plus the cross-platform roundtrip.

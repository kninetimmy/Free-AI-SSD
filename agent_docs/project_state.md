# Project State

Last updated: 2026-05-12 (latest) (PR #268 C27 Stage 2 merged `e807e04` — Hugging Face pull integration + disk-budget warnings. Stage 1 refusal removed from `PrepViewModel.DownloadAsync`; HF rows now pull through Ollama's native `hf.co/owner/repo` syntax (default-quant pick). New `HuggingFaceCatalogService.FetchSiblingsAsync` against `/api/models/{repoId}` (separate `AllowedModelDetailsPrefixes` allowlist + per-repo cache); new `PickSizingFile` static helper picks Q4_K_M first, falls back to smallest GGUF, sums multi-part `-NNNNN-of-NNNNN.gguf` series. WPF: VM exposes a `HuggingFaceSizingWarningsHook` delegate the view-host wires to the new fetch (keeps shared/ free of a prep-core dependency); warnings flow through the existing `ConfirmSizingWarnings` dialog. Mac: `mac-prep-host` `PullModelAsync` arm sizes HF tags off real `siblings[].lfs.size` bytes instead of `ModelSizingCatalog`'s 10 GB default; gated/private repos refuse early with Stage-3 pointer. ~20 new test pins. CI green first run. Previous: PR #266 C27 Stage 1 merged `58f79a1` — HF catalog + Source dropdown; PR #264 C25+C26 merged `3f299fe` — picker filter cluster polish.)

Last released: **v1.3.22** (2026-05-10; F2a — full-page picker, search, Most popular cap, both OSes). **v1.3.23 dispatch in flight** (run 25645859600, cross-OS) — bundles M11/C1/M12/M13/C2/M14/C3+C4+C5. **C24/C25/C26/C27 Stage 1+Stage 2 (PRs #262/#264/#266/#268) roll into the next dispatch** (v1.3.24).

> v1.2.7 tag exists on `af77abc` but has no GH release artifact; v1.2.8 supersedes it.

**Currently between tasks.** PR #268 (`e807e04`) closed **C27
Stage 2** — HF pull integration + `siblings[].lfs.size`
disk-budget warnings landed on both OSes. HF rows in the picker
are now downloadable via Ollama's native `hf.co/...` syntax.
Remaining C27 work: **Stage 3** (token auth via encrypted-config
storage; private + gated repos), **Stage 4** (per-quant row
expansion + capability inference). User to choose whether to
chain Stage 3 next or pivot to **P2 C6** (PrepApp
detect-already-configured-drive). Back-burnered: **M2** (signing
+ notarization, awaiting Apple Developer cert renewal), **M10**
(Mac PrepApp finalize observability, cold-load wait acceptable
post-MAC39). Ancestor-walk consolidation into
`prep-core/BundleContentRoots` remains overdue.

## Recently shipped

- **PR #268 — C27 Stage 2 Hugging Face pull integration + disk-budget warnings — merged `e807e04` (2026-05-12).** No new release tag (rolls into next dispatch, v1.3.24). Stage 2 of 4 — anonymous public-repo pulls only; Stages 3/4 defer. **Service:** new `HuggingFaceCatalogService.FetchSiblingsAsync(repoId, ct)` against `https://huggingface.co/api/models/{repoId}` with separate `AllowedModelDetailsPrefixes` HTTPS-only allowlist + per-repo in-memory cache; new records `HuggingFaceModelDetails(RepoId, Siblings, Gated, Private)` + `HuggingFaceSiblingFile(Filename, SizeBytes?)`; polymorphic HF `gated` field ("auto"/"manual"/false/true) parsed via JsonElement. New static `PickSizingFile(siblings)`: Q4_K_M-first heuristic, smallest-GGUF fallback, multi-part `-NNNNN-of-NNNNN.gguf` summing so split Q4_K_M doesn't under-warn at 1/3 of real cost. **VM (WPF, shared/):** Stage 1 refusal block deleted; HF rows flow through `_modelService.PullModelAsync` unchanged. New `HuggingFaceSizingWarningsHook` delegate property (view-host-supplied) keeps the VM free of an `IHuggingFaceCatalogService` dependency — shared/ can't reference prep-core/ (circular). Hook receives bare repoIds (stripped of `hf.co/` prefix) + free-disk bytes, returns warning strings; null hook (test default) skips HF disk-budget warnings without refusing the pull. `ConfirmSizingWarningsIfNeededAsync` (renamed from sync) concats HF warnings with Ollama-side ones into the existing dialog. **WPF view-host:** `MainWindow.xaml.cs` wires `_viewModel.HuggingFaceSizingWarningsHook = FetchHuggingFaceSizingWarningsAsync` which walks repoIds, calls `FetchSiblingsAsync`, runs `PickSizingFile`, formats warnings with picked filename + total GB + 2× headroom check. Gated/private surface a Stage-3-pointer message; per-repo fetch failure falls through (logged, pull proceeds). **Mac sidecar:** `mac-prep-host/HostLifetime.cs` `PullModelAsync` arm detects `hf.co/` prefix and calls new `EstimateHuggingFaceSizeAsync` to fetch siblings + size off real bytes before staging-precheck (`OllamaModelStager.EnsureStagingFreeSpace` was previously getting 10 GB from `ModelSizingCatalog.Suggest` regardless of repo — wildly under-counts 70B GGUFs). Gated/private repos refuse early with `EmitResult("pull-model", new { ok = false, reason = "hf-gated"/"hf-private", ... })`. **Tests:** ~20 new pins. HF service: siblings allowlist guard, malformed repoId (`ArgumentException`), URL form, 401/429 status surface, cache reuse, parser polymorphic-gated cases, `PickSizingFile` Q4_K_M preference + smallest-GGUF fallback + multi-part summing + no-GGUF null + null-size handling. VM: Stage 1 refusal pin flipped to Stage 2 pass-through; hook receives correct stripped repoIds + free-disk bytes; hook throwing logs + proceeds; null-hook default flow; `StripHuggingFacePrefix` helper. CI green first run on all three jobs (windows-build 4m41s + mac-prep-build 1m3s + mac-runner-build 1m6s).

- **PR #266 — C27 Stage 1 Hugging Face as a model source — merged `58f79a1` (2026-05-11).** No new release tag (rolls into next dispatch, v1.3.24). Stage 1 of 4 — anonymous read only. New `HuggingFaceCatalogService` against HF's GGUF Search API; new `ModelSource` enum plumbed through shared models; Source `ComboBox` (WPF) / SwiftUI `Picker` (Mac) with 350ms search debounce; new `discover-hf-catalog` + `search-hf` IPC arms; shared `BuildCatalogEntries` helper backs all four catalog-emitting arms (C24 lesson cashed in). 24 HF-service pins + 4 VM pins + 9 Swift pins. CI green first run. Decision pinned in `project_decisions.md` (catalog projection shape — single discriminator field).

- **PR #264 — C25+C26 picker filter cluster polish — merged `3f299fe` (2026-05-11).** Bundles capability pass-through marker (Option A: per-row opacity 0.55 + tooltip gated on new `HasActiveCapabilityFilter` derived signal) + Most-popular limit dropdown (10/15/25/50, default 15). WPF `MultiDataTrigger` + Mac SwiftUI `.opacity()` + `.help()`. 4 C# pins + 5 Swift pins. CI green first run.

> Older entries (PR #173–#262 MAC1–MAC40 + F2a v1.3.22 + M11/C1/M12/M13/C2/M14 + C3+C4+C5 + C24 — all merged 2026-05-05 through 2026-05-11) pruned to keep this section bounded. Full descriptions live in `git log` and the per-item entries in `agent_docs/mac_project_backlog.md` + `agent_docs/project_decisions.md`.

## Next up

**Authoritative priority lives in `project_backlog.md`** under the 2026-05-10 P0–P5 ranking. Highlights:

1. **P0 field-test bug surface — cleared.** M11, C1, M12, M13, C2, M14 all merged this session-cluster.
2. **P1 F2a model-picker cluster — cleared** (C3+C4+C5 bundled in PR #259 `9f81bd5`).
3. **P1.5 picker filter cluster follow-ons — partial.** C24/C25/C26 + **C27 Stage 1+Stage 2** all closed (PRs #262, #264, #266, #268). Remaining C27 stages: **Stage 3** (token auth + private/gated repos), **Stage 4** (per-quant row expansion + capability inference).
4. **P2 substantive UX:** `C6` (PrepApp detect-configured-drive flow). User may pick this over C27 Stage 3 to break up multi-stage work.
5. **P3 critical pre-existing:** `C7` (was X9, encrypted config persistence), `C8` (was X10, document replacement consistency), `W1` (was X11, companion keyboard PTT), `C9` (was X12, download verify-before-move), `C10` (was F4 Stage 2, post-setup launch flow), `W2` (was F4 Stages 3-4, companion installer), `C11` (was B2, LAN discovery), `C12` (was R1 Stage 2, runner-cli slash-commands), `M7` (was MAC20, cross-platform release ZIP layout rework).
6. **P4 RAG audit batch** (slot when v1.3.x feature work resumes): `C13` (X18), `C14` (X15), `C15` (X19), `C16` (X20), `C17` (X22), `C18` (X23), `C19` (X17).
7. **Back-burnered per user:** `M2` (was MAC11, signing + notarization, awaiting Apple Developer cert renewal), `M10` (was MAC37, finalize observability, cold-load wait acceptable post-MAC39).
8. **Cleanup follow-up (now overdue):** fold `MacArtifactAvailability.EnumerateContentRoots`, `ArtifactStagingService.EnumerateBundledContentRoots`, and `PrereqService.EnumerateBundleRoots` into one shared `prep-core/BundleContentRoots` helper. All three are byte-identical ancestor-walk enumerators.

**RAG audit backlog:** X17-X23 cover audit findings; X10/X13/X15 scope expansions recorded. Plan: `C:\Users\Kninetimmy\.claude\plans\okay-i-want-to-glowing-galaxy.md`. v1.3.x sequence: X18 -> X15 (expanded) -> X19 -> X20 -> X22 -> X23. X17 reduced to Stage 1 textless-page diagnostic (full OCR deferred — workload is text-layer PDFs).

**Dormant (could not reproduce):** X1-Redux. Diag branch `diag/x1-redux-send-hang` stays on remote, unmerged.

See `project_backlog.md` for full general backlog details. See
`agent_docs/mac_project_backlog.md` for the macOS support track.

## Last session

2026-05-12 (latest, C27 Stage 2 session) (PR #268 merged `e807e04`). User picked C27 Stage 2 from the remaining-stages cluster. Explicit Opus planning pass produced a 10-section plan covering 4 open decisions (D1 default quant, D2 lazy vs eager sibling fetch, D3 disk-budget formula, D4 multi-quant pick) + R1-R3 risks. User approved Lazy/Q4_K_M/Sum-multi-part. **Implementation surprise:** shared/ can't reference prep-core/ (circular dep — prep-core already references shared); first attempt to inject `IHuggingFaceCatalogService` into the VM ctor had to be reverted in favor of a `HuggingFaceSizingWarningsHook` delegate the view-host wires, mirroring how Stage 1 kept the HF service entirely in the view-host. On Mac the equivalent change lives in `mac-prep-host` (the C# sidecar) — Swift VM had no Stage 1 refusal to remove. Pre-existing test failures (4 — same set documented in earlier wrap-ups: Windows-only path/PS + mac-runner-host smoke); CI green first run on all three jobs. **Cross-OS audit (per parity rule):** both OSes wired to size off real bytes via the same shared service; Mac uses sized `EnsureStagingFreeSpace`, WPF uses the existing `ConfirmSizingWarnings` dialog. Decision candidate (asked separately): VM stays free of prep-core dependency via delegate hook; 2× headroom rule made explicit for HF pulls.

2026-05-11 (C27 Stage 1 session) (PR #266 merged `58f79a1`). User picked C27 from P1.5 follow-on cluster; explicit Opus planning pass produced a 10-section plan (6 decisions, file-by-file change list, HF API surface, cross-OS IPC bridge, UI placement, source-switch behavior, test plan, security-invariants checklist, out-of-scope, risks/unknowns). Implementation mirrored `LiveModelCatalogService`'s shape; `BuildCatalogEntries` shared helper cashes in the C24 lesson by collapsing four projection sites into one. 24+4 C# pins + 9 Swift pins. **Cross-OS audit:** both pickers got mirrored Source dropdowns + debounced searches + IPC arms — no parity gap. Decision pinned: multi-source catalogs use a single discriminator field on the shared entry, not parallel types.

> Older entries (C25+C26, C24, C3+C4+C5, M14, M11, C1, M12, M13, C2 — all 2026-05-10/11 merges) pruned to keep this section to the 2 most recent dated entries per the wrap-up rule. Full descriptions live in `git log` and the PR bodies.

## Open questions

- **v1.3.24 field-test pin (awaiting first run, both OSes; bundles C3/C4/C5/C24/C25/C26/C27 Stage 1+Stage 2):** **C3/C4/C5/C24 (Mac priority — F2a follow-ons reported here originally):** PrepApp picker → Refresh from Ollama → set Max size to ≤14B; rows above 14B disappear, configured + on-disk + bundled rows stay visible (null-params pass-through). Caption reads `Showing N of 399 matching filter (≤14B).` Click `tools` chip → capability AND filter narrows to tool-capable models only; combine with `vision` chip → only multi-cap entries survive (entries with empty caps still pass through so configured rows are unaffected). Pick Sort: Newest → entries reorder by scraped `x-test-updated` desc; nil-date entries (bundled, configured) sort last; caption gains ` Sorted by newest.` Pick Sort: A–Z → tag-ascending. **Windows parity:** Models tab → Refresh → same three controls in the second filter row, same caption strings, same pass-through posture for configured/on-disk rows. **Empty-data edge:** pre-Refresh on a freshly-prepped drive → capability chips and Max size cap are no-ops on bundled-only entries; Sort: Newest sorts only by what little data exists, nils last. **C25 marker:** with at least one chip engaged, rows with empty Capabilities render at opacity 0.55 with a tooltip; chips off → marker disappears. **C26 dropdown:** Most-popular limit dropdown switches between Top 10/15/25/50. **C27 Stage 1+Stage 2:** Source dropdown shows Ollama / Hugging Face; switching to HF clears the catalog and loads ~50 popular GGUF rows. Type "qwen" in search → debounced (350ms) `search-hf` fires. Capability chip + HF rows → all HF rows survive (empty caps + C25 marker). Sort: Newest → HF rows reorder by `lastModified`. **Stage 2 pull pin:** Check `Qwen/Qwen3-8B-GGUF` + click Download → temporary Ollama server starts, NDJSON progress renders `Pulling hf.co/Qwen/Qwen3-8B-GGUF…` with MB/s, completes, row flips to on-disk. **Stage 2 disk-budget pin:** check a row larger than free disk → confirm-dialog (WPF) / staging-precheck refusal (Mac) names the picked GGUF filename + total GB + headroom callout. **Stage 2 gated pin:** check a gated repo (e.g. a Llama-3.3 70B GGUF if gated) → friendly "Stage 3 token auth" error, no pull attempted. **Stage 2 multi-part pin:** pick a `-00001-of-NNNNN.gguf` repo → warning reports `(N-part split)` + summed size. 429 from the metadata fetch (rare) → hook logs "Could not fetch Hugging Face disk-budget data" and pull still proceeds. Switching source back to Ollama restores the bundled list immediately.

- **M14 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Mac Runner → unlock SSD → wait for `networkApiBaseUrl` to populate → Documents section → click "Pull embedding model". `libraryStatus` reads `Pulling embedding model…` then `Embedding model ready: nomic-embed-text:latest`. Force-quit Ollama via the runner's Stop control and click again → status surfaces the 503 reason (`Ollama host is not running. Start Ollama before pulling the embedding model.`). Companion-app parity: a Companion client with the same Bearer key can `POST /api/models/embedding/pull` and receive the same 200/503 shape. WPF runner unchanged → confirm by clicking the existing "Pull embedding model" button in the Index tools expander; behavior identical to pre-M14.

- **M11 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** PrepApp picker → Refresh from Ollama → click Most-popular ON. New accent-cyan line appears between the catalog status text and the picker reading `Showing top 15 of 399 by pulls.` (or whatever the live total is). Click OFF → line disappears. Type `llama` in search → reads `Showing N of 399 matching search.` Combine search + Most-popular → reads `Showing top N of 399 by pulls (filtered by search).` **Windows parity:** Models tab → Refresh → toggle Most popular. Same caption strings appear above the merged Models grid; configured + on-disk rows still surface (the top-15 cap only applies to Recommended-source rows, but the caption counts the entire visible ModelRows set). **Empty-catalog edge:** pre-Refresh on a freshly-prepped drive → caption stays empty under both toggle states (the existing "Refresh first" empty-state branch handles UX guidance).

- **C1 field-test pin (new — awaiting first run on a v1.3.23+ build, both OSes):** **Mac (priority — original symptom):** load `qwen3:14b` cold (force-quit Ollama first or pick a freshly-prepped SSD), Send → status reads `Loading qwen3:14b… 20s` then `…40s` then `…60s` etc. through the cold-load duration; flips to `Generating…` once the first token arrives; response area fills normally; final status `Answered` or `Answered with sources`. **SSD log evidence:** `chat stream begin (model=qwen3:14b, host=…)`, then a long gap, then `chat first-token in NNNNms (model=qwen3:14b)` and `chat stream complete in NNNNms`. **Warm-load contract:** second Send within 5 min of first → no `loading` frames in NDJSON, status goes `Sending... → Generating… → Answered`. **Windows parity:** same model + cold-load on Windows runner → `StreamingIndicator` reads `● Loading model… NNs` until tokens flow; PTT chat (which uses streaming under the hood) does NOT paint the loading text in the chat tab (visibility gate suppresses). **Stress pin:** if heartbeats keep flowing past 180s and no token ever arrives, that's the residual phase-1 case — Ollama-side load issue (memory pressure / corrupted blob); user can hit Stop and report. Heartbeat timer + write-gate prevent the silent-timeout class of bugs from ever recurring; this pin verifies the behavior end-to-end.

- **M12 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Send with empty model selected → red banner reads `"Model is required."` (validation 400 body). Force-quit `mac-runner-host` mid-request → red banner reads the URLSession transport reason (e.g., `"The network connection was lost."`); status strip also reads `Chat failed: ...`. Trigger a `RagRetrievalFailed` (e.g., temporarily delete the embedder) → orange `ragWarning` banner shows the RAG reason, response area shows the model's answer-without-context, no red banner. Mid-stream backend failure (e.g., kill ollama mid-stream) → red banner shows the `error` NDJSON frame message; file log gains `Chat backend error: <msg>`. Library ops with sidecar down → `libraryStatus` shows the bare reason (e.g., `"Service unavailable."`) without the now-removed `"Chat failed: "` prefix.

- **M13 field-test pin (new — awaiting first run on a v1.3.23+ mac build):** Click "Expose API on LAN" toggle ON → toggle stays ON; runner log shows `API host bound to <configured-bind>` (not `127.0.0.1`); `networkApiStatus` reads `API: <baseUrl>` with the LAN address. Click toggle OFF → toggle stays OFF; log shows `API host bound to 127.0.0.1`. Toggle ON, then force-quit the sidecar process from Activity Monitor → toggle reverts to OFF with `networkApiStatus` showing the crash message (proving `.crashed` path still clears intent). Lock + Unlock with toggle persisted ON in config → toggle hydrates ON and stays ON after auto-restart.

- **PR #247 C2 field-test pin (new — awaiting first run on a fresh-prepped Mac SSD):** drop a sub-1MB PDF into a new library → ingest succeeds end-to-end without manual embed-model setup. Re-finalize a fully-prepped drive → log shows `Embedding model nomic-embed-text:latest already on disk; skipping pull` and no temp Ollama server is spun up. Failure-message pin: temporarily delete the embedder from the SSD, re-ingest → error names `model 'nomic-embed-text' not found` rather than just the threshold ratio.

- **v1.3.22 mac + windows field-test pin (F2a) — released 2026-05-10 same session; awaiting first field run.** **Mac side now also covered by the M11 pin above** — the field-test confirms both the underlying filter behavior AND the new caption that announces the cap. **Mac picker smoke (priority):** PrepApp picker fills the page; resizing the window grows the list; search filters live as user types; clicking "Most popular" before Refresh shows the empty state with hint to Refresh first; after Refresh, Most popular shows ~15 entries sorted by pull count desc + the M11 caption announces the cap; clearing search/popular returns the full list and the caption hides. **Windows picker smoke:** Models tab gets the new search + Most popular row + M11 caption; same filter behavior; configured + on-disk rows stay visible even with Most popular on.
- **MAC37 driver pin (still applicable but back-burnered):** measure how long Mac PrepApp finalize takes on a fresh pull; observe whether it's actually doing work or waiting on a timeout. Not blocking — user accepted the 20s post-cache wait.
- **MAC35a/MAC34b/MAC35 deferred field-test smokes (still applicable on v1.3.22):** AppleDouble sidecar regression — re-running readiness on a v1.3.15-prepped SSD passes all-green; port 11434 reclaim — Ollama.app already running → Runner.app log shows "Found N existing process(es) on port 11434 (PIDs ...)"; pull cancel/resume scenarios — partials persist, no torn blobs after re-run; disk-full precheck fires when staging volume has < 2× model size free.
- **F2 manual smoke (partially complete on v1.3.5 mac field test):** Mac side confirmed at v1.3.5 + F2a at v1.3.22. **Windows side still deferred** to a Windows machine: online → click Refresh → grid repopulates; offline → caption explains failure, bundled list stays in place.
- **MAC20 still pending:** bundle root scatters Windows DLLs alongside `payload/` until MAC20 reshapes to `windows/` + `mac/` + `QUICKSTART.txt` + `LICENSE`.
- Before a public signed Mac beta, verify whether the nested `payload/mac/Runner.app.zip` (or whatever path MAC20 lands on) preserves the stapled notarization ticket after users download and extract the cross-platform ZIP. If not, ship a standalone notarized app ZIP or DMG.
- **MAC6 manual smoke (deferred until a Mac on the same LAN as a Windows Companion):** Windows Companion discovers and connects to a Mac-hosted Runner with Bearer auth + /api/health + /api/chat; Network Mode toggle with real Ollama serving a real model end-to-end.
- **MAC8 manual smoke (deferred to a real Mac):** Network Mode on → Documents UI populates → Create library → Add Files (TXT) → ingest completes → Send chat → returns sources from the uploaded file.
- **MAC10b manual visual smoke (deferred to a real Mac + a Windows machine):** Runner.app shows the Free-AI-SSD icon in Finder, the Dock, and command-tab on a clean Mac; WPF Runner / PrepApp / Companion show the icon in title bars and the Windows taskbar.
- **MAC17 manual smoke (deferred to a real Mac + an external SSD):** plug an external SSD → launch `PrepApp.app` → drive shows in selection list → choose Mac-only or cross-platform target → format → SSD layout → Mac Runner staged → Mac Ollama staged → starter model pulls → encryption setup with passphrase → readiness all green → finalize. Plus the cross-platform roundtrip.
